---
phase: 49.1-nfsv4-readdir-cookie-verifier
verified: 2026-03-12T16:42:00Z
status: human_needed
score: 4/4 must-haves verified
human_verification:
  - test: "Mount DittoFS via NFS on macOS and perform folder copy in Finder"
    expected: "No error -8062 during copy operation"
    why_human: "Requires real macOS NFS client integration and Finder UI interaction"
---

# Phase 49.1: NFSv4 READDIR Cookie Verifier - Verification Report

**Phase Goal:** Implement proper cookie verifier for NFSv4 READDIR instead of zero bytes
**Verified:** 2026-03-12T16:42:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | NFSv4 READDIR response for real-FS directories contains a non-zero 8-byte cookie verifier derived from directory mtime | ✓ VERIFIED | `directoryMtimeVerifier()` function exists (readdir.go:18-22), `GetFile()` fetches mtime (line 121), verifier encoded (line 172-174), test asserts non-zero (realfs_test.go:388-391) |
| 2 | NFSv4 READDIR response for pseudo-FS directories still returns zero verifier (no change) | ✓ VERIFIED | `readDirPseudoFS` writes zero bytes (readdir.go:271-272), regression test passes |
| 3 | Verifier mismatch on non-initial READDIR does NOT return NFS4ERR_NOT_SAME; entries are served normally with debug log | ✓ VERIFIED | Advisory validation logic (readdir.go:132-142) logs mismatch and continues, test verifies NFS4_OK returned (TestReadDir_RealFS_VerifierMismatch) |
| 4 | Paginated READDIR returns consistent verifier across pages for the same directory state | ✓ VERIFIED | Same `currentVerifier` used throughout response, test asserts consistency (realfs_test.go:552-554) |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `internal/adapter/nfs/v4/handlers/readdir.go` | Mtime-based cookie verifier generation and advisory validation for NFSv4 READDIR | ✓ VERIFIED | Contains `directoryMtimeVerifier()` (line 18-22), `GetFile()` call (line 121), verifier encoding (line 172-174), advisory validation (line 132-142) |
| `internal/adapter/nfs/v4/handlers/realfs_test.go` | Tests for non-zero verifier, verifier mismatch handling, and pagination consistency | ✓ VERIFIED | Contains `TestReadDir_RealFS_VerifierMismatch` (line 593), non-zero assertions in `TestReadDir_RealFS_ListsEntries` (line 388-391), pagination consistency in `TestReadDir_RealFS_PaginationWithTruncation` (line 552-554) |
| `internal/adapter/nfs/v4/handlers/ops_test.go` | Test helper for custom verifier in requests | ✓ VERIFIED | Contains `encodeReadDirWithVerf` helper (line 128-141) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `internal/adapter/nfs/v4/handlers/readdir.go` | `pkg/metadata` | `metaSvc.GetFile()` to fetch directory mtime for verifier | ✓ WIRED | Line 121: `dirFile, err := metaSvc.GetFile(authCtx.Context, metadata.FileHandle(ctx.CurrentFH))` |
| `internal/adapter/nfs/v4/handlers/readdir.go` | `encoding/binary` | `BigEndian.PutUint64/Uint64` for verifier encoding/decoding | ✓ WIRED | Line 135: `incomingVerf := binary.BigEndian.Uint64(cookieVerf[:])`, Line 173: `binary.BigEndian.PutUint64(verfBytes[:], currentVerifier)` |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| VERF-01 | 49.1-01-PLAN.md | READDIR response contains non-zero verifier for real-FS dirs | ✓ SATISFIED | Test `TestReadDir_RealFS_ListsEntries` asserts non-zero verifier (realfs_test.go:388-391), implementation verified in readdir.go:172-174 |
| VERF-02 | 49.1-01-PLAN.md | READDIR response contains zero verifier for pseudo-fs dirs | ✓ SATISFIED | `readDirPseudoFS` unchanged (readdir.go:271-272), test `TestRegression_PseudoFS_ReadDirStillWorks` passes |
| VERF-03 | 49.1-01-PLAN.md | Verifier mismatch does not return error (advisory only) | ✓ SATISFIED | Advisory validation implemented (readdir.go:132-142), test `TestReadDir_RealFS_VerifierMismatch` verifies NFS4_OK returned |
| VERF-04 | 49.1-01-PLAN.md | Pagination uses consistent verifier across pages | ✓ SATISFIED | Test `TestReadDir_RealFS_PaginationWithTruncation` verifies verifier consistency (realfs_test.go:552-554) |
| VERF-05 | 49.1-01-PLAN.md | macOS Finder folder copy works without error -8062 | ? NEEDS HUMAN | Requires manual testing with real macOS NFS client and Finder UI interaction |

### Anti-Patterns Found

None detected. Scanned files:
- `internal/adapter/nfs/v4/handlers/readdir.go` - No TODOs, FIXMEs, placeholders, empty implementations, or stub patterns
- `internal/adapter/nfs/v4/handlers/realfs_test.go` - All tests substantive with proper assertions
- `internal/adapter/nfs/v4/handlers/ops_test.go` - Helper functions complete

### Human Verification Required

#### 1. macOS Finder Folder Copy Without Error -8062

**Test:**
1. Start DittoFS server with NFSv4 adapter on default port (12049)
2. Mount the NFS share on macOS: `sudo mount -t nfs -o tcp,port=12049,mountport=12049,vers=4 localhost:/export /mnt/test`
3. Create a test directory with several files in `/mnt/test/testfolder/`
4. Use Finder to copy the folder (Cmd+C on folder, Cmd+V in same location or different location)
5. Observe the copy operation completes without error dialogs

**Expected:**
- Folder copy completes successfully
- No error -8062 ("The operation can't be completed because an unexpected error occurred (error code -8062)")
- All files in the copied folder are intact and readable

**Why human:**
Requires real macOS NFS client kernel integration and Finder UI interaction. Cannot be automated without a macOS virtual machine with GUI and NFS mount capabilities. The error -8062 is specific to macOS Finder's handling of NFSv4 READDIR pagination during folder copy operations.

### Gaps Summary

No gaps found. All automated verification passed:
- All 4 observable truths verified with concrete evidence
- All 3 required artifacts exist and are substantive (not stubs)
- Both key links verified and wired correctly
- 4 of 5 requirements satisfied (VERF-01 through VERF-04)
- No anti-patterns detected
- All unit tests pass (4/4 READDIR tests green)
- Full handler test suite passes
- `go build ./...` succeeds
- `go vet` passes with no warnings

The only remaining item is VERF-05 (macOS Finder verification), which requires manual testing due to its dependency on real macOS client behavior. This is an integration-level verification that cannot be automated without significant test infrastructure.

---

_Verified: 2026-03-12T16:42:00Z_
_Verifier: Claude (gsd-verifier)_
