---
phase: 49.1-nfsv4-readdir-cookie-verifier
plan: 01
subsystem: nfs
tags: [nfsv4, readdir, cookie-verifier, mtime, rfc7530]

# Dependency graph
requires:
  - phase: 49
    provides: "Complete v4.0 BlockStore unification with stable metadata service"
provides:
  - "Mtime-based NFSv4 READDIR cookie verifier replacing zero bytes"
  - "Advisory-only verifier mismatch validation with debug logging"
  - "directoryMtimeVerifier() helper in NFSv4 handlers"
affects: [49.2-readdirplus-performance]

# Tech tracking
tech-stack:
  added: []
  patterns: ["mtime-based cookie verifier for directory listing pagination"]

key-files:
  created: []
  modified:
    - "internal/adapter/nfs/v4/handlers/readdir.go"
    - "internal/adapter/nfs/v4/handlers/realfs_test.go"
    - "internal/adapter/nfs/v4/handlers/ops_test.go"

key-decisions:
  - "Mtime-based verifier matches NFSv3 pattern (uint64(mtime.UnixNano()))"
  - "Advisory-only validation: log mismatch at debug level, never return NFS4ERR_NOT_SAME"
  - "Pseudo-FS retains zero verifier (no change needed)"
  - "GetFile() called before ReadDirectory() for early verifier validation"

patterns-established:
  - "NFSv4 directoryMtimeVerifier() mirrors NFSv3 pattern exactly"
  - "Advisory verifier validation: cookie != 0 && incomingVerf != 0 && incomingVerf != current"

requirements-completed: [VERF-01, VERF-02, VERF-03, VERF-04, VERF-05]

# Metrics
duration: 6min
completed: 2026-03-12
---

# Phase 49.1 Plan 01: NFSv4 READDIR Cookie Verifier Summary

**Mtime-based 8-byte cookie verifier for NFSv4 READDIR with advisory-only mismatch validation, preventing macOS Finder error -8062**

## Performance

- **Duration:** 6 min
- **Started:** 2026-03-12T15:12:46Z
- **Completed:** 2026-03-12T15:18:50Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- NFSv4 READDIR now returns non-zero mtime-based cookie verifier for real-FS directories
- Verifier mismatch on non-initial requests logs at debug level but continues serving entries (no NFS4ERR_NOT_SAME)
- Paginated READDIR returns consistent verifier when directory is unchanged between pages
- Pseudo-FS READDIR unchanged (keeps zero verifier)
- Full test coverage including verifier non-zero, mismatch handling, and pagination consistency

## Task Commits

Each task was committed atomically:

1. **Task 1: Add verifier tests (TDD RED)** - `3df5afc7` (test)
2. **Task 2: Implement mtime-based verifier (TDD GREEN)** - `57c20bbd` (feat)

## Files Created/Modified
- `internal/adapter/nfs/v4/handlers/readdir.go` - Added directoryMtimeVerifier(), GetFile() for mtime, verifier encoding/validation
- `internal/adapter/nfs/v4/handlers/realfs_test.go` - Non-zero verifier assertion, pagination consistency, verifier mismatch test
- `internal/adapter/nfs/v4/handlers/ops_test.go` - encodeReadDirWithVerf helper for custom verifier in test requests

## Decisions Made
- Used `authCtx.Context` for `GetFile()` call (consistent with share routing context)
- Placed `directoryMtimeVerifier()` as package-level function in readdir.go (not a separate file)
- Updated `readDirRealFS` signature to accept `cookieVerf [8]byte` parameter from `handleReadDir`

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- NFSv4 READDIR cookie verifier complete
- Ready for Phase 49.2 (READDIRPLUS performance optimization)
- macOS Finder folder copy should work without error -8062 (requires manual verification: VERF-05)

---
*Phase: 49.1-nfsv4-readdir-cookie-verifier*
*Completed: 2026-03-12*
