---
phase: 73-smb-conformance-deep-dive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/adapter/smb/v2/handlers/change_notify.go
  - internal/adapter/smb/v2/handlers/change_notify_test.go
  - internal/adapter/smb/v2/handlers/set_info.go
  - internal/adapter/smb/v2/handlers/close.go
  - internal/adapter/smb/v2/handlers/create.go
  - internal/adapter/smb/v2/handlers/handler.go
  - internal/adapter/smb/response.go
  - test/smb-conformance/KNOWN_FAILURES.md
autonomous: true
requirements:
  - WPTS-01
  - WPTS-04

must_haves:
  truths:
    - "WPTS BVT_SMB2Basic_ChangeNotify_ChangeStreamName passes (ADS stream rename fires FILE_NOTIFY_CHANGE_STREAM_NAME)"
    - "WPTS BVT_SMB2Basic_ChangeNotify_ChangeStreamSize passes (ADS stream resize fires FILE_NOTIFY_CHANGE_STREAM_SIZE)"
    - "WPTS BVT_SMB2Basic_ChangeNotify_ChangeStreamWrite passes (ADS stream write fires FILE_NOTIFY_CHANGE_STREAM_WRITE)"
    - "WPTS BVT_SMB2Basic_ChangeNotify_ChangeSecurity passes (security descriptor change fires notification)"
    - "WPTS BVT_SMB2Basic_ChangeNotify_ServerReceiveSmb2Close passes (CLOSE on watched dir sends STATUS_NOTIFY_CLEANUP)"
  artifacts:
    - path: "internal/adapter/smb/v2/handlers/change_notify.go"
      provides: "ADS stream change notification constants and dispatch"
      contains: "FileNotifyChangeStreamName"
    - path: "internal/adapter/smb/v2/handlers/set_info.go"
      provides: "NotifyChange calls for ADS stream operations and security descriptor changes"
    - path: "internal/adapter/smb/v2/handlers/close.go"
      provides: "STATUS_NOTIFY_CLEANUP async response on CLOSE of watched directory"
  key_links:
    - from: "internal/adapter/smb/v2/handlers/set_info.go"
      to: "internal/adapter/smb/v2/handlers/change_notify.go"
      via: "NotifyRegistry.NotifyChange with stream and security filter flags"
      pattern: "NotifyChange.*FileNotifyChangeStream"
    - from: "internal/adapter/smb/v2/handlers/close.go"
      to: "internal/adapter/smb/response.go"
      via: "SendAsyncChangeNotifyResponse with STATUS_NOTIFY_CLEANUP"
      pattern: "StatusNotifyCleanup"
---

<objective>
Clear 5 WPTS BVT ChangeNotify failures by wiring ADS stream notifications, fixing ChangeSecurity async delivery, and fixing ServerReceiveSmb2Close cleanup response.

Purpose: D-01 mandates WPTS BVT first. These 5 tests represent the ChangeNotify subset of the 13 fixable WPTS failures. The stale watcher bug fix (D-04/D-09) is already committed on this branch.
Output: 5 fewer WPTS BVT known failures, ChangeNotify fully functional for all completion filter types.
</objective>

<execution_context>
@/Users/marmos91/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marmos91/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-smb-conformance-deep-dive/73-CONTEXT.md
@.planning/debug/smb-signing-mismatch.md
@.planning/debug/phase72-ci-failures.md
@internal/adapter/smb/v2/handlers/change_notify.go
@internal/adapter/smb/v2/handlers/set_info.go
@internal/adapter/smb/v2/handlers/close.go
@internal/adapter/smb/v2/handlers/create.go
@internal/adapter/smb/v2/handlers/handler.go
@internal/adapter/smb/response.go
@test/smb-conformance/KNOWN_FAILURES.md

<interfaces>
<!-- Key constants and types the executor needs -->

From internal/adapter/smb/v2/handlers/change_notify.go:
```go
// Existing completion filter constants:
FileNotifyChangeFileName   uint32 = 0x00000001
FileNotifyChangeDirName    uint32 = 0x00000002
FileNotifyChangeAttributes uint32 = 0x00000004
FileNotifyChangeSize       uint32 = 0x00000008
FileNotifyChangeLastWrite  uint32 = 0x00000010
FileNotifyChangeLastAccess uint32 = 0x00000020
FileNotifyChangeCreation   uint32 = 0x00000040
FileNotifyChangeSecurity   uint32 = 0x00000100
// MISSING — need to add:
// FileNotifyChangeStreamName  uint32 = 0x00000200
// FileNotifyChangeStreamSize  uint32 = 0x00000400
// FileNotifyChangeStreamWrite uint32 = 0x00000800
```

From internal/adapter/smb/types/status.go:
```go
StatusPending         Status = 0x00000103
StatusNotifyCleanup   Status = 0x0000010C  // Verify this exists; add if missing
StatusNotifyEnumDir   Status = 0x0000010D
```

From internal/adapter/smb/v2/handlers/handler.go:
```go
// NotifyRegistry field on Handler:
NotifyRegistry *NotifyRegistry
// closeFilesWithFilter already calls NotifyRegistry.Unregister (stale watcher fix applied)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ADS stream ChangeNotify notifications</name>
  <files>
    internal/adapter/smb/v2/handlers/change_notify.go
    internal/adapter/smb/v2/handlers/set_info.go
    internal/adapter/smb/v2/handlers/create.go
    internal/adapter/smb/v2/handlers/change_notify_test.go
  </files>
  <read_first>
    internal/adapter/smb/v2/handlers/change_notify.go
    internal/adapter/smb/v2/handlers/set_info.go
    internal/adapter/smb/v2/handlers/create.go
    internal/adapter/smb/v2/handlers/change_notify_test.go
    internal/adapter/smb/v2/handlers/handler.go
  </read_first>
  <action>
    1. In change_notify.go, add three new completion filter constants per MS-SMB2 2.2.35:
       - FileNotifyChangeStreamName  uint32 = 0x00000200
       - FileNotifyChangeStreamSize  uint32 = 0x00000400
       - FileNotifyChangeStreamWrite uint32 = 0x00000800

    2. In create.go, find ADS stream creation code (where streams are created as directory children with "file:stream:$DATA" naming). After successful ADS stream creation, call:
       h.NotifyRegistry.NotifyChange(parentDirFileID, FileNotifyChangeStreamName, FileActionAdded, streamDisplayName)
       where parentDirFileID is the parent directory's FileID and streamDisplayName is the stream name (e.g., ":streamname:$DATA").

    3. In set_info.go, find ADS stream rename handling. After successful stream rename, call:
       h.NotifyRegistry.NotifyChange(parentDirFileID, FileNotifyChangeStreamName, FileActionRenamedNewName, newStreamName)

    4. In set_info.go (or the write handler if stream writes go through there), find where ADS stream data is written/resized. After successful stream write, call:
       h.NotifyRegistry.NotifyChange(parentDirFileID, FileNotifyChangeStreamWrite, FileActionModified, streamName)
       After stream size change, also call:
       h.NotifyRegistry.NotifyChange(parentDirFileID, FileNotifyChangeStreamSize, FileActionModified, streamName)

    5. In set_info.go, find where security descriptors are set (FileSecurityInformation / SetSecurityDescriptor). After successful security descriptor change, call:
       h.NotifyRegistry.NotifyChange(parentDirFileID, FileNotifyChangeSecurity, FileActionModified, fileName)
       NOTE: The debug note says ChangeSecurity was prematurely removed from KNOWN_FAILURES. The fix is likely just adding this NotifyChange call — the async delivery infrastructure already works for other filter types.

    6. Update change_notify_test.go to add unit tests for:
       - Stream name notification fires on ADS create
       - Stream write notification fires on ADS write
       - Security notification fires on security descriptor change

    IMPORTANT: The NotifyChange call must use the PARENT DIRECTORY's FileID (the directory being watched), not the file's own FileID. WPTS registers a watcher on a directory and expects notifications when files/streams within that directory change.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs && go build ./... && go test ./internal/adapter/smb/... -count=1 -timeout 120s</automated>
  </verify>
  <acceptance_criteria>
    - change_notify.go contains `FileNotifyChangeStreamName  uint32 = 0x00000200`
    - change_notify.go contains `FileNotifyChangeStreamSize  uint32 = 0x00000400`
    - change_notify.go contains `FileNotifyChangeStreamWrite uint32 = 0x00000800`
    - set_info.go contains `NotifyChange` call with `FileNotifyChangeSecurity`
    - create.go or set_info.go contains `NotifyChange` call with `FileNotifyChangeStreamName`
    - All unit tests pass: `go test ./internal/adapter/smb/... -count=1` exits 0
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>ADS stream operations fire ChangeNotify with correct completion filter flags. Security descriptor changes fire FileNotifyChangeSecurity. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ServerReceiveSmb2Close notify cleanup and update KNOWN_FAILURES</name>
  <files>
    internal/adapter/smb/v2/handlers/close.go
    internal/adapter/smb/response.go
    internal/adapter/smb/types/status.go
    test/smb-conformance/KNOWN_FAILURES.md
  </files>
  <read_first>
    internal/adapter/smb/v2/handlers/close.go
    internal/adapter/smb/response.go
    internal/adapter/smb/types/status.go
    test/smb-conformance/KNOWN_FAILURES.md
    internal/adapter/smb/v2/handlers/change_notify.go
  </read_first>
  <action>
    1. In types/status.go, verify StatusNotifyCleanup exists (0x0000010C). If missing, add:
       StatusNotifyCleanup Status = 0x0000010C
       And add its String() case: `case StatusNotifyCleanup: return "STATUS_NOTIFY_CLEANUP"`

    2. In close.go, find the CLOSE handler. When a directory handle with a pending ChangeNotify watcher is closed (CLOSE command, not session cleanup):
       - Before unregistering the watcher from NotifyRegistry, send an async STATUS_NOTIFY_CLEANUP response to the pending CHANGE_NOTIFY request.
       - The async response must use the same AsyncId that was returned in the STATUS_PENDING interim response.
       - Per MS-SMB2 3.3.5.19, when the server receives a CLOSE for a directory with an outstanding CHANGE_NOTIFY:
         a) Send async response with Status=STATUS_NOTIFY_CLEANUP, AsyncId matching the pending request
         b) Then unregister the watcher
         c) Then proceed with normal CLOSE response
       - Look at the existing code near line 348-366 where NotifyRegistry.Unregister is called. Before that unregister, check if there's a pending watcher callback. If so, invoke SendAsyncChangeNotifyResponse with a ChangeNotifyResponse that has Status=STATUS_NOTIFY_CLEANUP and empty notification buffer.

    3. In response.go, check SendAsyncChangeNotifyResponse handles StatusNotifyCleanup correctly — it should send a response with the status code and no FILE_NOTIFY_INFORMATION entries (empty body or error body). The WPTS test likely expects exactly this status code on the async response when the watched directory is closed.

    4. In KNOWN_FAILURES.md, remove these 5 tests from the Expected Failures table:
       - BVT_SMB2Basic_ChangeNotify_ChangeStreamName
       - BVT_SMB2Basic_ChangeNotify_ChangeStreamSize
       - BVT_SMB2Basic_ChangeNotify_ChangeStreamWrite
       - BVT_SMB2Basic_ChangeNotify_ChangeSecurity
       - BVT_SMB2Basic_ChangeNotify_ServerReceiveSmb2Close

    5. Update the counts in KNOWN_FAILURES.md:
       - "Current baseline" line: change from "65 known failures (52 permanent + 13 expected)" to "60 known failures (52 permanent + 8 expected)"
       - "Total expected failures (fixable)" line: change from "13 tests" to "8 tests"
       - "WPTS BVT expected failures (primary gate)" line: change from 13 to 8
       - "Grand total known failures" line: change from "65 tests (52 permanent + 13 expected)" to "60 tests (52 permanent + 8 expected)"
       - ChangeNotify category count: reduce from 4 to 0 (remove the ChangeNotify row from the "Remaining Expected Failure Categories" table since ChangeEa is permanent, not expected)
       - Move BVT_SMB2Basic_ChangeNotify_ChangeEa from "Expected" status to "Permanent" status (since D-07 says EA is genuinely unfixable)
       - Add changelog entry for Phase 73

    NOTE: Do NOT remove the 9 ADS share access tests yet — those are handled in Plan 02.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs && go build ./... && go test ./internal/adapter/smb/... -count=1 -timeout 120s</automated>
  </verify>
  <acceptance_criteria>
    - types/status.go contains `StatusNotifyCleanup` with value `0x0000010C`
    - close.go contains logic to send STATUS_NOTIFY_CLEANUP async response before unregistering watchers on CLOSE
    - KNOWN_FAILURES.md does NOT contain `BVT_SMB2Basic_ChangeNotify_ChangeStreamName`
    - KNOWN_FAILURES.md does NOT contain `BVT_SMB2Basic_ChangeNotify_ChangeSecurity`
    - KNOWN_FAILURES.md does NOT contain `BVT_SMB2Basic_ChangeNotify_ServerReceiveSmb2Close`
    - KNOWN_FAILURES.md contains "60 known failures" or "60 tests"
    - KNOWN_FAILURES.md contains `BVT_SMB2Basic_ChangeNotify_ChangeEa` with status "Permanent"
    - `go build ./...` exits 0
    - `go test ./internal/adapter/smb/... -count=1` exits 0
  </acceptance_criteria>
  <done>ServerReceiveSmb2Close sends STATUS_NOTIFY_CLEANUP on directory CLOSE. 5 ChangeNotify tests removed from KNOWN_FAILURES. ChangeEa marked permanent. All code compiles and tests pass.</done>
</task>

</tasks>

<verification>
- `go build ./...` compiles without errors
- `go test ./internal/adapter/smb/... -count=1 -timeout 120s` passes
- `grep -c "ChangeNotify" test/smb-conformance/KNOWN_FAILURES.md` returns 1 (only ChangeEa remains, now Permanent)
- `grep "FileNotifyChangeStreamName" internal/adapter/smb/v2/handlers/change_notify.go` returns a match
- `grep "StatusNotifyCleanup" internal/adapter/smb/types/status.go` returns a match
</verification>

<success_criteria>
5 WPTS BVT ChangeNotify tests cleared from known failures. ADS stream operations fire correct notifications. Security descriptor changes fire notifications. CLOSE on watched directory sends STATUS_NOTIFY_CLEANUP. Code compiles and all unit tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/73-smb-conformance-deep-dive/73-01-SUMMARY.md`
</output>
