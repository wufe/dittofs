---
phase: 69-smb-protocol-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/adapter/smb/v2/handlers/negotiate.go
  - internal/adapter/smb/v2/handlers/negotiate_test.go
  - internal/adapter/smb/v2/handlers/session_setup.go
  - internal/adapter/smb/crypto_state_conformance_test.go
  - internal/adapter/smb/framing.go
  - internal/adapter/smb/compound.go
  - internal/adapter/smb/response.go
  - test/smb-conformance/KNOWN_FAILURES.md
autonomous: true
requirements: [SMB-01]

must_haves:
  truths:
    - "SMB 3.1.1 NEGOTIATE response sets SecurityMode to include SMB2_NEGOTIATE_SIGNING_REQUIRED (0x02) when dialect is 3.1.1"
    - "SESSION_SETUP for 3.1.1 dialects enforces NegSigningRequired regardless of config"
    - "Non-3.1.1 dialects (3.0, 3.0.2) honor the SigningConfig.Required setting without override"
    - "Unsigned unencrypted post-auth requests on 3.1.1 connections are rejected per MS-SMB2 3.3.5.2.4"
    - "Compound command signature verification works for all 3.x dialects"
  artifacts:
    - path: "internal/adapter/smb/v2/handlers/negotiate.go"
      provides: "3.1.1 NegSigningRequired enforcement in NEGOTIATE response"
      contains: "Dialect0311"
    - path: "internal/adapter/smb/v2/handlers/session_setup.go"
      provides: "3.1.1 NegSigningRequired enforcement in SESSION_SETUP"
      contains: "Dialect0311"
    - path: "internal/adapter/smb/crypto_state_conformance_test.go"
      provides: "Signing conformance tests covering audit findings"
  key_links:
    - from: "internal/adapter/smb/v2/handlers/negotiate.go"
      to: "internal/adapter/smb/types/constants.go"
      via: "Dialect0311 constant check"
      pattern: "Dialect0311"
    - from: "internal/adapter/smb/v2/handlers/session_setup.go"
      to: "internal/adapter/smb/session/session.go"
      via: "EnableSigning(true) for 3.1.1"
      pattern: "EnableSigning"
---

<objective>
Fix macOS SMB 3.1.1 signing by absorbing PR #288 and conducting a full signing audit against MS-SMB2 spec sections 3.3.x.

Purpose: macOS mount_smbfs fails because DittoFS does not enforce NegSigningRequired for SMB 3.1.1 dialect, causing signature verification mismatches. PR #288 fixes the core issue. The signing audit ensures no other signing-related spec violations exist across negotiate, session setup, tree connect, re-auth, and compound signing paths.

Output: Working macOS signing, comprehensive signing conformance tests, updated KNOWN_FAILURES.md reflecting WPTS improvements.
</objective>

<execution_context>
@/Users/marmos91/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marmos91/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-smb-protocol-foundation/69-CONTEXT.md
@.planning/phases/69-smb-protocol-foundation/69-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From internal/adapter/smb/types/constants.go:
```go
const Dialect0311 uint16 = 0x0311
```

From internal/adapter/smb/session/session.go:
```go
func (s *Session) EnableSigning(required bool)
func (s *Session) SetCryptoState(cs *SessionCryptoState)
func (s *Session) ShouldSign() bool
func (s *Session) ShouldVerify() bool
func (s *Session) SignMessage(message []byte)
func (s *Session) VerifyMessage(message []byte) bool
```

From internal/adapter/smb/v2/handlers/handler.go:
```go
type Handler struct {
    SigningConfig    SigningConfig
    EncryptionConfig EncryptionConfig
    // ...
}
type SigningConfig struct {
    Enabled  bool
    Required bool
}
```

From internal/adapter/smb/crypto_state.go:
```go
type ConnectionCryptoState struct { /* preauth hash chain */ }
func (c *ConnectionCryptoState) GetDialect() uint16
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Absorb PR #288 and extend signing enforcement</name>
  <files>
    internal/adapter/smb/v2/handlers/negotiate.go,
    internal/adapter/smb/v2/handlers/negotiate_test.go,
    internal/adapter/smb/v2/handlers/session_setup.go,
    internal/adapter/smb/crypto_state_conformance_test.go
  </files>
  <read_first>
    internal/adapter/smb/v2/handlers/negotiate.go,
    internal/adapter/smb/v2/handlers/negotiate_test.go,
    internal/adapter/smb/v2/handlers/session_setup.go,
    internal/adapter/smb/crypto_state_conformance_test.go,
    internal/adapter/smb/session/session.go,
    internal/adapter/smb/session/crypto_state.go,
    internal/adapter/smb/types/constants.go
  </read_first>
  <behavior>
    - Test: NEGOTIATE response for dialect 3.1.1 has SecurityMode with SMB2_NEGOTIATE_SIGNING_REQUIRED bit (0x02) set regardless of SigningConfig.Required
    - Test: NEGOTIATE response for dialect 3.0 does NOT force SMB2_NEGOTIATE_SIGNING_REQUIRED when SigningConfig.Required=false
    - Test: SESSION_SETUP for dialect 3.1.1 creates session with SigningRequired=true
    - Test: SESSION_SETUP for dialect 3.0.2 with SigningConfig.Required=false creates session with SigningRequired=false
    - Test: Preauth integrity hash chain is correctly computed across NEGOTIATE + SESSION_SETUP sequence for 3.1.1
  </behavior>
  <action>
Cherry-pick the changes from the `fix/smb3-signing` branch (PR #288) into the current branch. The core changes are:

1. In `negotiate.go`: After dialect negotiation, if the selected dialect is `types.Dialect0311` (0x0311), force `SecurityMode |= SMB2_NEGOTIATE_SIGNING_REQUIRED` (0x02) in the NEGOTIATE response, regardless of `handler.SigningConfig.Required`. For non-3.1.1 dialects, keep the existing behavior that honors `SigningConfig.Required`.

2. In `session_setup.go`: When completing session setup and the connection's negotiated dialect is 3.1.1 (check via `ctx.ConnCryptoState.GetDialect() == types.Dialect0311`), call `session.EnableSigning(true)` to force `SigningRequired=true` on the session. For non-3.1.1 dialects, use the handler's `SigningConfig.Required` value.

3. In `negotiate_test.go`: Add test cases for dialect-conditional signing enforcement. Test that 3.1.1 always gets signing required, 3.0 respects config. Use the existing negotiate test structure.

4. In `crypto_state_conformance_test.go`: Add or update conformance tests covering the preauth hash chain computation. Verify that the hash is correctly updated across NEGOTIATE request -> NEGOTIATE response -> SESSION_SETUP request -> SESSION_SETUP response for 3.1.1 connections.

Use `git cherry-pick` or manual application of the 4 commits from `fix/smb3-signing` branch. Resolve any merge conflicts by preferring the PR #288 changes for signing-specific code and the current branch for unrelated changes.

References:
- MS-SMB2 section 3.3.5.4 (NEGOTIATE signing rules)
- MS-SMB2 section 3.3.5.5 (SESSION_SETUP signing rules)
- PR #288 branch: `fix/smb3-signing` (4 commits, 13 files)
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs && go test ./internal/adapter/smb/v2/handlers/ -run TestNegotiate -count=1 -v && go test ./internal/adapter/smb/... -run TestCryptoState -count=1 -v</automated>
  </verify>
  <acceptance_criteria>
    - negotiate.go contains a conditional check for `Dialect0311` that forces signing required (grep: `Dialect0311` in negotiate.go)
    - session_setup.go contains dialect-conditional signing enforcement (grep: `Dialect0311` in session_setup.go)
    - negotiate_test.go contains test function `TestNegotiate.*311.*Signing` or similar pattern
    - crypto_state_conformance_test.go exists and has test functions covering preauth hash chain
    - `go test ./internal/adapter/smb/v2/handlers/ -count=1` exits 0
    - `go test ./internal/adapter/smb/... -count=1` exits 0
  </acceptance_criteria>
  <done>
    NEGOTIATE response for 3.1.1 always includes SMB2_NEGOTIATE_SIGNING_REQUIRED.
    SESSION_SETUP for 3.1.1 always creates sessions with SigningRequired=true.
    Non-3.1.1 dialects honor SigningConfig without override.
    All existing and new signing tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full signing audit — verify and fix all MS-SMB2 3.3.x signing paths</name>
  <files>
    internal/adapter/smb/framing.go,
    internal/adapter/smb/response.go,
    internal/adapter/smb/compound.go,
    test/smb-conformance/KNOWN_FAILURES.md
  </files>
  <read_first>
    internal/adapter/smb/framing.go,
    internal/adapter/smb/response.go,
    internal/adapter/smb/compound.go,
    internal/adapter/smb/v2/handlers/negotiate.go,
    internal/adapter/smb/v2/handlers/session_setup.go,
    internal/adapter/smb/v2/handlers/tree_connect.go,
    internal/adapter/smb/signing/sign.go,
    internal/adapter/smb/session/session.go,
    internal/adapter/smb/session/crypto_state.go,
    test/smb-conformance/KNOWN_FAILURES.md
  </read_first>
  <action>
Conduct a comprehensive signing audit against MS-SMB2 spec. For each area below, read the current code and verify compliance. Fix any violations found.

**Audit checklist (verify each and fix if non-compliant):**

1. **MS-SMB2 3.3.5.2.4 — Signing verification on incoming requests:**
   - Verify that `framing.go` (or wherever incoming message signature is checked) verifies signatures on ALL post-auth requests when `session.ShouldVerify()` is true
   - Verify that for SMB 3.1.1, unsigned+unencrypted requests from non-guest, non-null sessions are rejected (disconnect per spec; per user decision: error response, not disconnect)
   - Check: Does the current code in `framing.go` handle the 3.1.1 mandatory signing case?

2. **MS-SMB2 3.3.4.1.1 — Signing outgoing responses:**
   - Verify that `SendMessage()` in `response.go` signs ALL responses when `session.ShouldSign()` is true and session is not encrypted
   - Verify that encrypted sessions use AEAD instead of signing (already in code — confirm no bypass)
   - Confirm that SESSION_SETUP SUCCESS response for newly created sessions is signed but NOT encrypted

3. **MS-SMB2 3.3.5.7 — TREE_CONNECT:**
   - Verify tree connect does not require special signing handling (it doesn't per spec, but verify no signing state is incorrectly modified)

4. **MS-SMB2 3.3.5.2.7.2 — Compound signing:**
   - Verify `VerifyCompoundCommandSignature()` in `compound.go` verifies each compound command individually over its own bytes (header to NextCommand)
   - Verify that `sendCompoundResponses()` signs each response command individually before concatenation

5. **MS-SMB2 3.3.5.5.3 — Re-authentication signing:**
   - Verify that re-auth SESSION_SETUP on an existing session preserves signing keys until new ones are derived
   - Verify the `NewlyCreated` flag correctly distinguishes new vs re-auth sessions

6. **Cross-reference with Samba source:**
   - Check Samba's `source3/smb2_signing.c` and `smbd/smb2_server.c` for any edge cases around signing order, which commands skip signing, and compound signing behavior
   - Document any findings as code comments

For each audit finding that requires a code change, add a code comment referencing the MS-SMB2 section number.

After all fixes, update `test/smb-conformance/KNOWN_FAILURES.md` to remove any WPTS BVT tests that now pass due to signing fixes (expected: ~5 `BVT_Negotiate_SMB311*` tests may move from known failures to passing).
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs && go build ./... && go test ./internal/adapter/smb/... -count=1 -race</automated>
  </verify>
  <acceptance_criteria>
    - response.go SendMessage function has comment referencing "MS-SMB2 3.3.4.1.1" for signing logic
    - compound.go VerifyCompoundCommandSignature has comment referencing "MS-SMB2 3.3.5.2.7.2"
    - framing.go (or relevant file) has signing verification for incoming messages referencing "MS-SMB2 3.3.5.2.4"
    - `go build ./...` exits 0
    - `go test ./internal/adapter/smb/... -count=1 -race` exits 0
    - KNOWN_FAILURES.md updated (file modification timestamp changed)
  </acceptance_criteria>
  <done>
    All MS-SMB2 3.3.x signing paths audited with spec references in code comments.
    Any signing violations found and fixed.
    KNOWN_FAILURES.md updated to reflect tests that now pass.
    No regressions in existing tests.
  </done>
</task>

</tasks>

<verification>
- `go build ./...` compiles without errors
- `go test ./internal/adapter/smb/... -count=1 -race` all pass
- `go test ./internal/adapter/smb/v2/handlers/ -run TestNegotiate -count=1` passes
- Signing conformance tests pass
</verification>

<success_criteria>
- SMB 3.1.1 NEGOTIATE always includes SMB2_NEGOTIATE_SIGNING_REQUIRED
- SMB 3.1.1 SESSION_SETUP always creates sessions with SigningRequired=true
- Non-3.1.1 dialects respect SigningConfig settings (no regression)
- All MS-SMB2 3.3.x signing paths have spec-referenced code comments
- KNOWN_FAILURES.md updated for newly passing tests
</success_criteria>

<output>
After completion, create `.planning/phases/69-smb-protocol-foundation/69-01-SUMMARY.md`
</output>
