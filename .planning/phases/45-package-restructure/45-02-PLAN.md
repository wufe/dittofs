---
phase: 45-package-restructure
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - pkg/blockstore/local/fs/fs.go
  - pkg/blockstore/local/fs/read.go
  - pkg/blockstore/local/fs/write.go
  - pkg/blockstore/local/fs/flush.go
  - pkg/blockstore/local/fs/eviction.go
  - pkg/blockstore/local/fs/manage.go
  - pkg/blockstore/local/fs/recovery.go
  - pkg/blockstore/local/fs/block.go
  - pkg/blockstore/local/fs/types.go
  - pkg/blockstore/local/fs/fdcache.go
  - pkg/blockstore/local/fs/fadvise_linux.go
  - pkg/blockstore/local/fs/fadvise_other.go
  - pkg/blockstore/local/fs/fs_test.go
  - pkg/blockstore/local/fs/manage_test.go
  - pkg/blockstore/local/fs/doc.go
  - pkg/blockstore/local/memory/memory.go
  - pkg/blockstore/local/memory/memory_test.go
  - pkg/blockstore/local/memory/doc.go
  - pkg/blockstore/local/localtest/suite.go
  - pkg/blockstore/local/localtest/doc.go
  - pkg/blockstore/remote/s3/store.go
  - pkg/blockstore/remote/s3/store_test.go
  - pkg/blockstore/remote/memory/store.go
  - pkg/blockstore/remote/memory/store_test.go
  - pkg/blockstore/remote/remotetest/suite.go
  - pkg/blockstore/remote/remotetest/doc.go
  - pkg/blockstore/sync/syncer.go
  - pkg/blockstore/sync/sync.go
  - pkg/blockstore/sync/fetch.go
  - pkg/blockstore/sync/dedup.go
  - pkg/blockstore/sync/queue.go
  - pkg/blockstore/sync/entry.go
  - pkg/blockstore/sync/types.go
  - pkg/blockstore/sync/doc.go
  - pkg/blockstore/sync/syncer_test.go
  - pkg/blockstore/sync/nil_remotestore_test.go
  - pkg/blockstore/sync/queue_test.go
  - pkg/blockstore/sync/entry_test.go
  - pkg/blockstore/gc/gc.go
  - pkg/blockstore/gc/gc_test.go
  - pkg/blockstore/gc/gc_integration_test.go
  - pkg/blockstore/gc/doc.go
autonomous: true
requirements: [PKG-03, PKG-04, PKG-05, PKG-06, PKG-07, PKG-08]

must_haves:
  truths:
    - "FSStore (renamed from BlockCache) compiles and tests pass at pkg/blockstore/local/fs/"
    - "MemoryLocalStore exists as full implementation at pkg/blockstore/local/memory/"
    - "S3 remote store compiles at pkg/blockstore/remote/s3/"
    - "Memory remote store compiles and tests pass at pkg/blockstore/remote/memory/"
    - "Syncer (renamed from Offloader) compiles and tests pass at pkg/blockstore/sync/"
    - "GC compiles and tests pass at pkg/blockstore/gc/"
    - "LocalStore conformance suite runs against both fs/ and memory/ implementations"
    - "RemoteStore conformance suite runs against memory/ implementation"
  artifacts:
    - path: "pkg/blockstore/local/fs/fs.go"
      provides: "FSStore struct implementing local.LocalStore"
      contains: "type FSStore struct"
    - path: "pkg/blockstore/local/memory/memory.go"
      provides: "MemoryStore implementing local.LocalStore for tests"
      contains: "type MemoryStore struct"
    - path: "pkg/blockstore/remote/s3/store.go"
      provides: "S3Store implementing remote.RemoteStore"
      contains: "type S3Store struct"
    - path: "pkg/blockstore/sync/syncer.go"
      provides: "Syncer struct (renamed from Offloader)"
      contains: "type Syncer struct"
    - path: "pkg/blockstore/gc/gc.go"
      provides: "Collector for block garbage collection"
      contains: "type Collector struct"
    - path: "pkg/blockstore/local/localtest/suite.go"
      provides: "LocalStore conformance test suite"
      exports: ["RunConformanceSuite"]
    - path: "pkg/blockstore/remote/remotetest/suite.go"
      provides: "RemoteStore conformance test suite"
      exports: ["RunConformanceSuite"]
  key_links:
    - from: "pkg/blockstore/local/fs/fs.go"
      to: "pkg/blockstore/local"
      via: "implements local.LocalStore interface"
      pattern: "var _ local\\.LocalStore"
    - from: "pkg/blockstore/local/memory/memory.go"
      to: "pkg/blockstore/local"
      via: "implements local.LocalStore interface"
      pattern: "var _ local\\.LocalStore"
    - from: "pkg/blockstore/remote/s3/store.go"
      to: "pkg/blockstore/remote"
      via: "implements remote.RemoteStore interface"
      pattern: "var _ remote\\.RemoteStore"
    - from: "pkg/blockstore/sync/syncer.go"
      to: "pkg/blockstore/local"
      via: "uses local.LocalStore for cache operations"
      pattern: "local\\.Local"
    - from: "pkg/blockstore/sync/syncer.go"
      to: "pkg/blockstore/remote"
      via: "uses remote.RemoteStore for block transfers"
      pattern: "remote\\.RemoteStore"
---

<objective>
Move all implementation packages to their new locations: cache to local/fs, payload stores to remote/, offloader to sync/, gc to gc/. Create MemoryLocalStore and conformance test suites.

Purpose: Physically relocate all implementation code into the new pkg/blockstore/ hierarchy. After this plan, all implementation code lives under pkg/blockstore/ but consumers still import old paths (updated in Plan 4).

Output: Complete implementation hierarchy under pkg/blockstore/ with all tests passing at new locations.
</objective>

<execution_context>
@/Users/marmos91/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marmos91/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-package-restructure/45-CONTEXT.md
@.planning/phases/45-package-restructure/45-RESEARCH.md
@.planning/phases/45-package-restructure/45-01-SUMMARY.md

<interfaces>
<!-- Interfaces from Plan 01 that implementations must satisfy -->

From pkg/blockstore/local/local.go (created in Plan 01):
```go
type LocalReader interface {
    ReadAt(ctx context.Context, payloadID string, dest []byte, offset uint64) (bool, error)
    GetFileSize(ctx context.Context, payloadID string) (uint64, bool)
    IsBlockCached(ctx context.Context, payloadID string, blockIdx uint64) bool
    GetBlockData(ctx context.Context, payloadID string, blockIdx uint64) ([]byte, uint32, error)
}
type LocalWriter interface {
    WriteAt(ctx context.Context, payloadID string, data []byte, offset uint64) error
    WriteFromRemote(ctx context.Context, payloadID string, data []byte, offset uint64) error
}
type LocalFlusher interface {
    Flush(ctx context.Context, payloadID string) ([]FlushedBlock, error)
    GetDirtyBlocks(ctx context.Context, payloadID string) ([]PendingBlock, error)
    SyncFileBlocks(ctx context.Context)
    SyncFileBlocksForFile(ctx context.Context, payloadID string)
}
type LocalManager interface { /* lifecycle, delete, truncate, state transitions, stats */ }
type LocalStore interface { LocalReader; LocalWriter; LocalFlusher; LocalManager }
```

From pkg/blockstore/remote/remote.go (created in Plan 01):
```go
type RemoteStore interface {
    WriteBlock(ctx, blockKey string, data []byte) error
    ReadBlock(ctx, blockKey string) ([]byte, error)
    ReadBlockRange(ctx, blockKey string, offset, length int64) ([]byte, error)
    DeleteBlock(ctx, blockKey string) error
    DeleteByPrefix(ctx, prefix string) error
    ListByPrefix(ctx, prefix string) ([]string, error)
    Close() error
    HealthCheck(ctx) error
}
```

From pkg/blockstore/types.go (created in Plan 01):
```go
type FileBlock struct { ... }
type BlockState uint8
type ContentHash [32]byte
const BlockSize = 8 * 1024 * 1024
func FormatStoreKey(payloadID string, blockIdx uint64) string
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move cache to local/fs, create memory local store, and local conformance suite</name>
  <files>
    pkg/blockstore/local/fs/fs.go
    pkg/blockstore/local/fs/read.go
    pkg/blockstore/local/fs/write.go
    pkg/blockstore/local/fs/flush.go
    pkg/blockstore/local/fs/eviction.go
    pkg/blockstore/local/fs/manage.go
    pkg/blockstore/local/fs/recovery.go
    pkg/blockstore/local/fs/block.go
    pkg/blockstore/local/fs/types.go
    pkg/blockstore/local/fs/fdcache.go
    pkg/blockstore/local/fs/fadvise_linux.go
    pkg/blockstore/local/fs/fadvise_other.go
    pkg/blockstore/local/fs/fs_test.go
    pkg/blockstore/local/fs/manage_test.go
    pkg/blockstore/local/fs/doc.go
    pkg/blockstore/local/memory/memory.go
    pkg/blockstore/local/memory/memory_test.go
    pkg/blockstore/local/memory/doc.go
    pkg/blockstore/local/localtest/suite.go
    pkg/blockstore/local/localtest/doc.go
  </files>
  <action>
**Move pkg/cache/ -> pkg/blockstore/local/fs/**:

Copy all 14 files from pkg/cache/ to pkg/blockstore/local/fs/. Apply these changes to every file:
- Change `package cache` to `package fs`
- Rename cache.go to fs.go
- Rename cache_test.go to fs_test.go
- Rename struct `BlockCache` to `FSStore`
- Rename constructor `New` to `New` (keep name, it's now `fs.New()`)
- Update all internal references: `BlockCache` -> `FSStore`, `bc` receiver can stay

Update imports in every moved file:
- Replace `"github.com/marmos91/dittofs/pkg/metadata"` with `"github.com/marmos91/dittofs/pkg/blockstore"` for FileBlock, BlockState, ContentHash types
- Replace `metadata.FileBlock` with `blockstore.FileBlock`, `metadata.BlockState*` with `blockstore.BlockState*`, etc.
- Replace local `BlockSize` constant references with `blockstore.BlockSize` (import from blockstore root)
- Replace local `FormatStoreKey` references with `blockstore.FormatStoreKey`
- For PendingBlock and FlushedBlock, import from `"github.com/marmos91/dittofs/pkg/blockstore/local"` since they are defined in local.go
- Remove the local BlockSize constant from types.go (now in blockstore root)
- Remove the local FormatStoreKey function from fs.go (now in blockstore root)
- Remove local PendingBlock, FlushedBlock, Stats types from types.go (now in local/local.go)

Add interface satisfaction assertion in fs.go:
```go
var _ local.LocalStore = (*FSStore)(nil)
```

Import the local package: `"github.com/marmos91/dittofs/pkg/blockstore/local"`

CRITICAL: The FSStore struct's `fileBlockStore` field currently references `metadata.FileBlockStore`. Update to `blockstore.FileBlockStore`.

CRITICAL: Inside pkg/blockstore/local/fs/, when referencing Go's standard library `sync` package, use the normal `"sync"` import. There's no naming collision because this package is `fs`, not `sync`.

**Create pkg/blockstore/local/memory/memory.go** -- Full MemoryLocalStore:
- Package `memory`
- Implements local.LocalStore interface fully (not a stub)
- Uses in-memory maps for block data: map[string]map[uint64][]byte (payloadID -> blockIdx -> data)
- Thread-safe with sync.RWMutex
- Tracks file sizes, dirty state, block states
- Implements all LocalReader, LocalWriter, LocalFlusher, LocalManager methods
- Add interface assertion: `var _ local.LocalStore = (*MemoryStore)(nil)`

**Create pkg/blockstore/local/memory/memory_test.go**:
- Run the conformance suite against MemoryStore
- Add a few memory-specific unit tests

**Create pkg/blockstore/local/localtest/suite.go** -- Conformance suite:
- Package `localtest`
- Define `type LocalStoreFactory func(t *testing.T) local.LocalStore`
- Function `RunConformanceSuite(t *testing.T, factory LocalStoreFactory)` testing:
  - WriteAt then ReadAt round-trip
  - GetFileSize returns correct size
  - Flush produces FlushedBlocks
  - Truncate changes size
  - EvictMemory clears in-memory state
  - DeleteAllBlockFiles removes data
  - Close is safe to call
- Focus on interface contract, not implementation details

**Create pkg/blockstore/local/localtest/doc.go**: Package doc.
**Create pkg/blockstore/local/memory/doc.go**: Package doc.
**Create pkg/blockstore/local/fs/doc.go**: Package doc.

Wire fs_test.go to also run the conformance suite at the end of the test file.

DO NOT delete pkg/cache/ yet -- that happens in Plan 4.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs45 && go build ./pkg/blockstore/local/... && go test ./pkg/blockstore/local/fs/ && go test ./pkg/blockstore/local/memory/ && go test ./pkg/blockstore/local/localtest/...</automated>
  </verify>
  <done>
    - pkg/blockstore/local/fs/ contains moved cache code, compiles, tests pass
    - FSStore satisfies local.LocalStore interface (compile-time check)
    - pkg/blockstore/local/memory/ has full MemoryStore implementation
    - MemoryStore satisfies local.LocalStore interface
    - pkg/blockstore/local/localtest/ conformance suite runs against both implementations
    - All types reference blockstore root (no more metadata imports for block types)
  </done>
</task>

<task type="auto">
  <name>Task 2: Move remote stores, offloader to sync, GC, and create remote conformance suite</name>
  <files>
    pkg/blockstore/remote/s3/store.go
    pkg/blockstore/remote/s3/store_test.go
    pkg/blockstore/remote/memory/store.go
    pkg/blockstore/remote/memory/store_test.go
    pkg/blockstore/remote/remotetest/suite.go
    pkg/blockstore/remote/remotetest/doc.go
    pkg/blockstore/sync/syncer.go
    pkg/blockstore/sync/sync.go
    pkg/blockstore/sync/fetch.go
    pkg/blockstore/sync/dedup.go
    pkg/blockstore/sync/queue.go
    pkg/blockstore/sync/entry.go
    pkg/blockstore/sync/types.go
    pkg/blockstore/sync/doc.go
    pkg/blockstore/sync/syncer_test.go
    pkg/blockstore/sync/nil_remotestore_test.go
    pkg/blockstore/sync/queue_test.go
    pkg/blockstore/sync/entry_test.go
    pkg/blockstore/gc/gc.go
    pkg/blockstore/gc/gc_test.go
    pkg/blockstore/gc/gc_integration_test.go
    pkg/blockstore/gc/doc.go
  </files>
  <action>
**Move pkg/payload/store/s3/ -> pkg/blockstore/remote/s3/**:
- Copy store.go and store_test.go
- Package name stays `s3`
- Update the import of `"github.com/marmos91/dittofs/pkg/payload/store"` to `"github.com/marmos91/dittofs/pkg/blockstore/remote"`
- The S3Store currently implements `store.BlockStore`. Update to implement `remote.RemoteStore`
- Add: `var _ remote.RemoteStore = (*S3Store)(nil)`
- Replace `store.ErrBlockNotFound` with `blockstore.ErrBlockNotFound` (import blockstore root)
- Replace `store.ErrStoreClosed` with `blockstore.ErrStoreClosed`

**Move pkg/payload/store/memory/ -> pkg/blockstore/remote/memory/**:
- Copy store.go and store_test.go
- Package name stays `memory`
- Same import updates as S3: `remote.RemoteStore` instead of `store.BlockStore`
- Add: `var _ remote.RemoteStore = (*Store)(nil)`
- Update error references

**Create pkg/blockstore/remote/remotetest/suite.go** -- Conformance suite:
- Package `remotetest`
- Define `type RemoteStoreFactory func(t *testing.T) remote.RemoteStore`
- Function `RunConformanceSuite(t *testing.T, factory RemoteStoreFactory)` testing:
  - WriteBlock then ReadBlock round-trip
  - ReadBlockRange returns correct slice
  - DeleteBlock makes ReadBlock return ErrBlockNotFound
  - DeleteByPrefix removes matching keys
  - ListByPrefix returns correct keys
  - HealthCheck succeeds
  - Close is safe
- Create doc.go

Wire remote/memory store_test.go to also run the conformance suite.

**Move pkg/payload/offloader/ -> pkg/blockstore/sync/** with renames:

File renames per CONTEXT.md:
| offloader.go -> syncer.go | upload.go -> sync.go | download.go -> fetch.go |
| dedup.go -> dedup.go | queue.go -> queue.go | entry.go -> entry.go | types.go -> types.go |
| offloader_test.go -> syncer_test.go | nil_blockstore_test.go -> nil_remotestore_test.go |
| queue_test.go -> queue_test.go | entry_test.go -> entry_test.go | doc.go -> doc.go |

Package rename: `package offloader` -> `package sync`

Type/method renames per CONTEXT.md:
- Offloader -> Syncer
- New() (constructor stays New, becomes sync.New())
- uploadPendingBlocks -> syncLocalBlocks
- downloadBlock -> fetchBlock
- processUploadResult -> processSyncResult
- fileUploadState -> fileSyncState
- downloadResult -> fetchResult
- uploadResult -> syncResult
- TransferQueue -> SyncQueue
- TransferQueueEntry -> SyncQueueEntry

Import updates:
- Replace `"github.com/marmos91/dittofs/pkg/cache"` with `"github.com/marmos91/dittofs/pkg/blockstore/local"` (for LocalFlusher, PendingBlock types) AND `"github.com/marmos91/dittofs/pkg/blockstore/local/fs"` ONLY if concrete FSStore is referenced (likely not -- syncer uses interfaces)
- Replace `"github.com/marmos91/dittofs/pkg/payload/store"` with `"github.com/marmos91/dittofs/pkg/blockstore/remote"` (for RemoteStore)
- Replace `"github.com/marmos91/dittofs/pkg/metadata"` with `"github.com/marmos91/dittofs/pkg/blockstore"` (for FileBlock, FileBlockStore, BlockState, FormatStoreKey)
- Replace `cache.FormatStoreKey` with `blockstore.FormatStoreKey`
- Replace `cache.BlockCache` parameter type with `local.LocalFlusher` (or broader local.LocalStore if needed)
- Replace `store.BlockStore` with `remote.RemoteStore`
- Replace `metadata.FileBlockStore` with `blockstore.FileBlockStore`

CRITICAL: Inside the `sync` package, Go's standard library `sync` package must be aliased:
```go
import gosync "sync"  // use gosync.WaitGroup, gosync.Mutex, gosync.Map, etc.
```

Check EVERY file in the sync package for `sync.WaitGroup`, `sync.Mutex`, `sync.Map`, `sync.Once`, `sync.RWMutex`, `sync.Pool`, `sync.Cond`, `sync.Locker` and replace with `gosync.*`.

Also check for `"sync/atomic"` -- this does NOT need aliasing (it's a different package path).

**Move pkg/payload/gc/ -> pkg/blockstore/gc/**:
- Copy all 4 files
- Package name stays `gc`
- Update imports:
  - Replace `"github.com/marmos91/dittofs/pkg/metadata"` with `"github.com/marmos91/dittofs/pkg/blockstore"` for FileBlock, FileBlockStore
  - Replace `"github.com/marmos91/dittofs/pkg/payload/store"` with `"github.com/marmos91/dittofs/pkg/blockstore/remote"` for RemoteStore
- Preserve `//go:build integration` tag in gc_integration_test.go

DO NOT delete old directories yet -- that happens in Plan 4.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs45 && go build ./pkg/blockstore/... && go test ./pkg/blockstore/remote/memory/ && go test ./pkg/blockstore/sync/ && go test ./pkg/blockstore/gc/</automated>
  </verify>
  <done>
    - pkg/blockstore/remote/s3/ compiles with RemoteStore interface
    - pkg/blockstore/remote/memory/ compiles and tests pass
    - pkg/blockstore/remote/remotetest/ conformance suite passes for memory
    - pkg/blockstore/sync/ compiles and all tests pass with renamed types/methods
    - All sync package files use gosync alias for Go's standard sync
    - pkg/blockstore/gc/ compiles and tests pass (excluding integration)
    - go build ./pkg/blockstore/... succeeds for entire hierarchy
  </done>
</task>

</tasks>

<verification>
1. `go build ./pkg/blockstore/...` entire blockstore hierarchy compiles
2. `go test ./pkg/blockstore/local/fs/` -- FSStore tests pass
3. `go test ./pkg/blockstore/local/memory/` -- MemoryStore tests pass
4. `go test ./pkg/blockstore/remote/memory/` -- remote memory tests pass
5. `go test ./pkg/blockstore/sync/` -- Syncer tests pass
6. `go test ./pkg/blockstore/gc/` -- GC tests pass
7. Conformance suites run for both local and remote implementations
</verification>

<success_criteria>
- All 6 implementation sub-packages exist under pkg/blockstore/
- FSStore satisfies local.LocalStore (compile-time verified)
- MemoryStore (local) satisfies local.LocalStore
- S3Store satisfies remote.RemoteStore
- MemoryStore (remote) satisfies remote.RemoteStore
- Syncer compiles with all renames applied
- GC compiles with updated imports
- All existing tests pass at new locations
- Conformance suites verify interface contracts
</success_criteria>

<output>
After completion, create `.planning/phases/45-package-restructure/45-02-SUMMARY.md`
</output>
