---
phase: 73-smb-conformance-deep-dive
plan: 05
type: execute
wave: 3
depends_on:
  - 73-01
  - 73-02
  - 73-03
  - 73-04
files_modified:
  - internal/adapter/smb/v2/handlers/stub_handlers.go
  - internal/adapter/smb/compound.go
  - internal/adapter/smb/v2/handlers/set_info.go
  - test/smb-conformance/smbtorture/KNOWN_FAILURES.md
  - test/smb-conformance/KNOWN_FAILURES.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - WPTS-02
  - WPTS-04

must_haves:
  truths:
    - "smbtorture compound edge case tests pass (D-16)"
    - "smbtorture timestamp freeze-thaw test passes"
    - "ROADMAP.md Phase 73 success criteria updated to revised targets (D-05)"
    - "All KNOWN_FAILURES counts are accurate and consistent"
    - "Zero new failures in both WPTS and smbtorture (all fixed tests removed from known failures, no regressions)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 73 success criteria with revised targets"
      contains: "WPTS BVT known failures"
    - path: "test/smb-conformance/KNOWN_FAILURES.md"
      provides: "Accurate WPTS BVT failure counts"
    - path: "test/smb-conformance/smbtorture/KNOWN_FAILURES.md"
      provides: "Accurate smbtorture failure counts"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "test/smb-conformance/KNOWN_FAILURES.md"
      via: "Success criteria numbers match actual known failure counts"
      pattern: "known failures"
---

<objective>
Final sweep: fix compound edge cases, timestamp freeze-thaw, update ROADMAP success criteria to revised targets (D-05), and ensure all KNOWN_FAILURES files are accurate and consistent.

Purpose: D-05 requires ROADMAP update. D-16 covers compound edge cases (3 tests). This plan also catches any remaining fixes from Plans 01-04 and ensures all documentation is consistent.
Output: ROADMAP updated, all KNOWN_FAILURES consistent, compound edge cases fixed, freeze-thaw fixed.
</objective>

<execution_context>
@/Users/marmos91/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marmos91/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/73-smb-conformance-deep-dive/73-CONTEXT.md
@.planning/phases/73-smb-conformance-deep-dive/73-01-SUMMARY.md
@.planning/phases/73-smb-conformance-deep-dive/73-02-SUMMARY.md
@.planning/phases/73-smb-conformance-deep-dive/73-03-SUMMARY.md
@.planning/phases/73-smb-conformance-deep-dive/73-04-SUMMARY.md
@internal/adapter/smb/compound.go
@internal/adapter/smb/v2/handlers/stub_handlers.go
@internal/adapter/smb/v2/handlers/set_info.go
@test/smb-conformance/KNOWN_FAILURES.md
@test/smb-conformance/smbtorture/KNOWN_FAILURES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix compound edge cases and timestamp freeze-thaw</name>
  <files>
    internal/adapter/smb/compound.go
    internal/adapter/smb/v2/handlers/stub_handlers.go
    internal/adapter/smb/v2/handlers/set_info.go
  </files>
  <read_first>
    internal/adapter/smb/compound.go
    internal/adapter/smb/v2/handlers/stub_handlers.go
    internal/adapter/smb/v2/handlers/set_info.go
    .planning/debug/phase72-ci-failures.md
  </read_first>
  <action>
    **Compound Edge Cases (D-16, 3 tests):**

    1. Read the compound request tests from smbtorture (source4/torture/smb2/compound.c) to understand exact expectations.

    2. In compound.go, check for these edge cases:
       a) Compound request where a middle operation fails — subsequent operations in the compound should receive STATUS_INVALID_PARAMETER (not be dispatched)
       b) Compound request with related operations (using 0xFFFFFFFF as FileID in subsequent ops) — the FileID from the previous CREATE must be propagated
       c) Compound request credit accounting — credits charged at compound level (first request's CreditCharge), granted in last response

    3. These tests were described in CONTEXT as "newly reachable, likely simple parameter validation fixes". Check:
       - Does compound.go correctly propagate STATUS_INVALID_PARAMETER when a prior operation fails?
       - Does the related FileID (0xFFFFFFFF) substitution work for all command types, not just CREATE->READ?
       - Is error propagation working correctly (if CREATE fails, subsequent QUERY_INFO with related FileID gets error)?

    **Timestamp Freeze-Thaw (1 test: smb2.timestamps.freeze-thaw):**

    4. Per the debug note in phase72-ci-failures.md:
       - Test sets CreationTime explicitly, freezes it (-1), then thaws (-2), checking create_time stays unchanged
       - The branch changed sentinel pin from isFiletimeSentinel (both -1 and -2) to filetimeFreeze-only (-1), with separate unfreeze (-2) setting Mtime to time.Now()
       - This causes side effects: when unfreezing (-2), the Ctime auto-update cascade may inadvertently modify CreationTime

    5. Fix in set_info.go:
       - When processing -2 (unfreeze) for a specific timestamp field, ONLY unfreeze that field and set it to time.Now()
       - Do NOT let the unfreeze operation trigger auto-updates on OTHER timestamp fields
       - Specifically: unfreezing Mtime should NOT auto-update Ctime or CreationTime
       - Check: the frozen state tracking per-field. Each timestamp (CreationTime, LastAccessTime, LastWriteTime, ChangeTime) should have independent freeze/unfreeze state
       - If the freeze tracking is global (one flag for all), refactor to per-field freeze tracking
       - The CreationTime drift (~716ms per debug note) suggests that the unfreeze of another field is triggering a SetFileAttributes call that auto-updates CreationTime as a side effect

    6. Verify the fix handles these scenarios:
       - Set CreationTime to specific value -> Freeze CreationTime (-1) -> Unfreeze Mtime (-2) -> CreationTime should be unchanged
       - Set Mtime to specific value -> Freeze Mtime (-1) -> Modify file -> Mtime should stay frozen
       - Unfreeze Mtime (-2) -> Next modification should update Mtime normally
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs && go build ./... && go test ./internal/adapter/smb/... -count=1 -timeout 120s</automated>
  </verify>
  <acceptance_criteria>
    - compound.go correctly propagates failure status to subsequent related operations
    - set_info.go has per-field freeze tracking (not global) for CreationTime, LastAccessTime, LastWriteTime, ChangeTime
    - set_info.go unfreeze (-2) only affects the target field, not other frozen fields
    - `go build ./...` exits 0
    - `go test ./internal/adapter/smb/... -count=1` exits 0
  </acceptance_criteria>
  <done>Compound edge cases fixed (error propagation, related FileID substitution). Timestamp freeze-thaw fixed with per-field freeze tracking. All code compiles and tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP, REQUIREMENTS, and finalize KNOWN_FAILURES</name>
  <files>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    test/smb-conformance/KNOWN_FAILURES.md
    test/smb-conformance/smbtorture/KNOWN_FAILURES.md
  </files>
  <read_first>
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
    test/smb-conformance/KNOWN_FAILURES.md
    test/smb-conformance/smbtorture/KNOWN_FAILURES.md
    .planning/phases/73-smb-conformance-deep-dive/73-01-SUMMARY.md
    .planning/phases/73-smb-conformance-deep-dive/73-02-SUMMARY.md
    .planning/phases/73-smb-conformance-deep-dive/73-03-SUMMARY.md
    .planning/phases/73-smb-conformance-deep-dive/73-04-SUMMARY.md
  </read_first>
  <action>
    **ROADMAP Update (D-05):**

    1. In .planning/ROADMAP.md, find the Phase 73 section and update:
       - Success Criteria item 5: Change "WPTS BVT known failures at or below 35" to "WPTS BVT known failures at or below 53 (52 permanent + 1 ChangeEa permanent; all fixable expected failures cleared)"
       - Success Criteria item 5: Change "smbtorture known failures reduced" to "smbtorture known failures reduced from 492 to ~460"
       - Update Plans: "5 plans" with plan list:
         - [ ] 73-01-PLAN.md -- WPTS ChangeNotify completion (ADS stream, ChangeSecurity, ServerReceiveSmb2Close)
         - [ ] 73-02-PLAN.md -- WPTS ADS share access + timestamp conformance
         - [ ] 73-03-PLAN.md -- smbtorture ChangeNotify + session re-auth + anonymous encryption
         - [ ] 73-04-PLAN.md -- smbtorture durable handles + leases
         - [ ] 73-05-PLAN.md -- Compound edge cases + freeze-thaw + documentation

    **REQUIREMENTS Update:**

    2. In .planning/REQUIREMENTS.md, update the WPTS requirement statuses:
       - WPTS-01: Update from "Pending" to note: "Phase 73: ChangeNotify fully implemented"
       - WPTS-02: Update from "Pending" to note: "Phase 73: Negotiate edge cases + compound fixes"
       - WPTS-03: Update from "Pending" to note: "Phase 73: Lease/DH fixes"
       - WPTS-04: Update from "Pending" to note: "Phase 73: Known failures reduced"
       - Update Traceability table: WPTS-01 through WPTS-04 from "Phase 72 | Pending" to "Phase 73 | Complete"

    **KNOWN_FAILURES Finalization:**

    3. Read the current state of both KNOWN_FAILURES.md files (after Plans 01-04 have modified them).

    4. For WPTS KNOWN_FAILURES.md:
       - Verify the count is accurate: count all test entries in the table
       - Verify there are NO "Expected" status entries (all should be "Permanent")
       - Update the "Current baseline" line with accurate Phase 73 numbers
       - Ensure the Phase 73 changelog entry covers all changes from Plans 01-02

    5. For smbtorture KNOWN_FAILURES.md:
       - Count actual remaining test entries (grep -c "^| smb2\.")
       - Update any summary counts at top/bottom of file
       - Ensure the Phase 73 changelog entry covers all changes from Plans 03-04
       - Add compound and freeze-thaw removals from this plan

    6. Remove from smbtorture KNOWN_FAILURES.md:
       - smb2.timestamps.freeze-thaw (fixed in Task 1)
       - Any compound tests fixed in Task 1 (verify which ones actually pass)
       - Add Phase 73 Plan 05 changelog entry

    **Cross-Verification:**

    7. Verify consistency between:
       - ROADMAP success criteria numbers ↔ KNOWN_FAILURES actual counts
       - REQUIREMENTS status ↔ actual implementation state
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs && go build ./... && grep -c "Expected" test/smb-conformance/KNOWN_FAILURES.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 73 contains "5 plans" with plan list
    - ROADMAP.md Phase 73 success criteria mentions "~53" or "52 permanent" for WPTS BVT
    - ROADMAP.md Phase 73 success criteria mentions "~460" for smbtorture
    - REQUIREMENTS.md Traceability table shows WPTS-01 through WPTS-04 as "Phase 73 | Complete"
    - WPTS KNOWN_FAILURES.md has 0 "Expected" status entries
    - smbtorture KNOWN_FAILURES.md does NOT contain `smb2.timestamps.freeze-thaw`
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>ROADMAP updated with revised targets per D-05. REQUIREMENTS traceability updated. Both KNOWN_FAILURES files accurate and consistent. All documentation reflects actual implementation state.</done>
</task>

</tasks>

<verification>
- `go build ./...` compiles without errors
- `go test ./internal/adapter/smb/... -count=1 -timeout 120s` passes
- ROADMAP.md Phase 73 has "5 plans" and updated success criteria
- REQUIREMENTS.md shows WPTS-01 through WPTS-04 as Complete
- `grep -c "Expected" test/smb-conformance/KNOWN_FAILURES.md` returns 0
</verification>

<success_criteria>
Compound edge cases fixed. Timestamp freeze-thaw fixed with per-field tracking. ROADMAP and REQUIREMENTS updated with accurate Phase 73 targets and status. All KNOWN_FAILURES files are accurate, consistent, and match ROADMAP numbers. Zero new failures introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/73-smb-conformance-deep-dive/73-05-SUMMARY.md`
</output>
