---
phase: 39-cross-protocol-integration-and-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/metadata/lock/delegation.go
  - pkg/metadata/lock/delegation_test.go
  - pkg/metadata/lock/types.go
  - pkg/metadata/lock/manager.go
  - pkg/metadata/lock/oplock_break.go
  - pkg/metadata/lock/store.go
  - pkg/metadata/lock/notification_queue.go
  - pkg/metadata/lock/notification_queue_test.go
  - pkg/metadata/lock/directory.go
autonomous: true
requirements: [XPROT-01, XPROT-03]

must_haves:
  truths:
    - "Delegation struct exists in pkg/metadata/lock/ with no NFS-specific fields"
    - "UnifiedLock has a *Delegation field alongside existing *OpLock"
    - "BreakCallbacks interface includes OnDelegationRecall method"
    - "LockManager has CheckAndBreakCachingForWrite/Read/Delete methods that break both leases and delegations"
    - "OpLockBreakScanner scans delegation recall timeouts alongside lease break timeouts"
    - "Bounded notification queue with overflow collapse exists for directory change events"
    - "PersistedLock can serialize/deserialize delegation data for LockStore"
  artifacts:
    - path: "pkg/metadata/lock/delegation.go"
      provides: "Protocol-neutral Delegation struct, DelegationType enum, grant/recall/revoke helpers"
      min_lines: 80
    - path: "pkg/metadata/lock/notification_queue.go"
      provides: "Bounded NotificationQueue with overflow collapse, typed DirNotification events"
      min_lines: 80
    - path: "pkg/metadata/lock/delegation_test.go"
      provides: "Unit tests for Delegation lifecycle, coexistence rules, break coordination"
      min_lines: 60
    - path: "pkg/metadata/lock/notification_queue_test.go"
      provides: "Unit tests for notification queue capacity, overflow, flush"
      min_lines: 50
  key_links:
    - from: "pkg/metadata/lock/types.go"
      to: "pkg/metadata/lock/delegation.go"
      via: "UnifiedLock.Delegation *Delegation field"
      pattern: "Delegation\\s+\\*Delegation"
    - from: "pkg/metadata/lock/manager.go"
      to: "pkg/metadata/lock/delegation.go"
      via: "CheckAndBreakCachingFor* calls breakDelegations"
      pattern: "CheckAndBreakCachingFor"
    - from: "pkg/metadata/lock/oplock_break.go"
      to: "pkg/metadata/lock/delegation.go"
      via: "BreakCallbacks.OnDelegationRecall dispatch"
      pattern: "OnDelegationRecall"
---

<objective>
Create the protocol-neutral delegation foundation in pkg/metadata/lock/: the Delegation struct, UnifiedLock extension, BreakCallbacks interface extension, unified CheckAndBreakCaching methods, extended OpLockBreakScanner, bounded notification queue, and LockStore serialization. This establishes all shared types and methods that Plan 02 will wire into adapters.

Purpose: Delegations must become a first-class lock type in the shared LockManager before NFS/SMB adapters can use them. This plan creates all the building blocks without touching adapter code.

Output: Extended lock package with delegation support, notification queue, and comprehensive unit tests.
</objective>

<execution_context>
@/Users/marmos91/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marmos91/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-cross-protocol-integration-and-documentation/39-CONTEXT.md
@.planning/phases/39-cross-protocol-integration-and-documentation/39-RESEARCH.md
@.planning/phases/37-smb3-leases-and-directory-leasing/37-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From pkg/metadata/lock/types.go:
```go
type UnifiedLock struct {
    ID         string
    Owner      LockOwner
    FileHandle FileHandle
    Offset     uint64
    Length     uint64
    LockType   LockType
    Lease      *OpLock      // SMB2/3 lease state (nil for byte-range)
    // NOTE: *Delegation field will be added by this plan
}
```

From pkg/metadata/lock/oplock_break.go:
```go
type BreakCallbacks interface {
    OnOpLockBreak(handleKey string, lock *UnifiedLock, breakToState uint32)
    OnByteRangeRevoke(handleKey string, lock *UnifiedLock, reason string)
    OnAccessConflict(handleKey string, existingLock *UnifiedLock, requestedMode AccessMode)
    // NOTE: OnDelegationRecall will be added by this plan
}
```

From pkg/metadata/lock/manager.go (LockManager interface, relevant methods):
```go
CheckAndBreakOpLocksForWrite(handleKey string, excludeOwner *LockOwner) error
CheckAndBreakOpLocksForRead(handleKey string, excludeOwner *LockOwner) error
CheckAndBreakOpLocksForDelete(handleKey string, excludeOwner *LockOwner) error
```

From pkg/metadata/lock/oplock.go (OpLock pattern to follow):
```go
type OpLock struct {
    LeaseKey       [16]byte
    LeaseState     uint32
    // ... other fields
}
```

From pkg/metadata/lock/store.go (PersistedLock pattern):
```go
type PersistedLock struct {
    ID         string
    HandleKey  string
    Owner      LockOwner
    // ... includes lease fields, will need delegation fields
}
```

From pkg/metadata/lock/directory.go:
```go
type DirChangeType int
const (
    DirChangeAddEntry    DirChangeType = iota
    DirChangeRemoveEntry
    DirChangeRenameEntry
)

type DirChangeNotifier interface {
    OnDirChange(parentHandle FileHandle, changeType DirChangeType, originClientID string)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Delegation struct, extend UnifiedLock and BreakCallbacks, add notification queue</name>
  <files>
    pkg/metadata/lock/delegation.go,
    pkg/metadata/lock/types.go,
    pkg/metadata/lock/oplock_break.go,
    pkg/metadata/lock/notification_queue.go,
    pkg/metadata/lock/store.go,
    pkg/metadata/lock/delegation_test.go,
    pkg/metadata/lock/notification_queue_test.go
  </files>
  <behavior>
    - Delegation struct has DelegationID, DelegType, IsDirectory, ClientID, ShareName, Breaking, BreakStarted, Recalled, Revoked, NotificationMask fields
    - DelegationType enum has DelegTypeRead and DelegTypeWrite
    - UnifiedLock.IsDelegation() returns true when Delegation != nil
    - UnifiedLock.Delegation field coexists with Lease (only one non-nil at a time)
    - BreakCallbacks interface has OnDelegationRecall(handleKey string, lock *UnifiedLock) method
    - DelegationConflictsWithLease: write delegation conflicts with any lease, read delegation only conflicts with write leases
    - NotificationQueue: bounded capacity (1024 default), Push adds events, overflow collapses to single "rescan needed" event
    - NotificationQueue: Drain returns all events and resets, Len returns count
    - NotificationQueue: flush channel signaled when threshold reached (100 events or timer-based)
    - PersistedLock can round-trip delegation fields (DelegationID, DelegType, IsDirectory, Breaking, Recalled, Revoked)
    - Delegation.Clone() returns deep copy
  </behavior>
  <action>
    1. Create `pkg/metadata/lock/delegation.go`:
       - `DelegationType` enum with `DelegTypeRead`, `DelegTypeWrite` constants
       - `Delegation` struct following the pattern from 39-RESEARCH.md Pattern 1 (protocol-neutral, NO NFS-specific fields like Stateid4 or RecallTimer)
       - `Delegation.Clone()` method returning deep copy
       - `DelegationConflictsWithLease(deleg *Delegation, lease *OpLock) bool` function implementing coexistence rules: read deleg + read lease = OK, write deleg + any lease = CONFLICT, any deleg + write lease = CONFLICT
       - `NewDelegation(delegType DelegationType, clientID, shareName string, isDirectory bool) *Delegation` constructor using uuid.New().String() for DelegationID

    2. Extend `pkg/metadata/lock/types.go`:
       - Add `Delegation *Delegation` field to `UnifiedLock` struct (alongside existing `Lease *OpLock`)
       - Add `IsDelegation() bool` method on UnifiedLock (returns `ul.Delegation != nil`)
       - Update `UnifiedLock.Clone()` to clone the Delegation field

    3. Extend `pkg/metadata/lock/oplock_break.go`:
       - Add `OnDelegationRecall(handleKey string, lock *UnifiedLock)` to `BreakCallbacks` interface
       - This WILL break existing implementations (SMBBreakHandler, any NFS callback). That's expected -- Plan 02 fixes them. For this plan, update any in-package test stubs/mocks that implement BreakCallbacks to add a no-op `OnDelegationRecall`.

    4. Create `pkg/metadata/lock/notification_queue.go`:
       - `DirNotification` struct with `ChangeType DirChangeType`, `EntryName string`, `OldName string`, `NewName string`
       - `NotificationQueue` struct with bounded capacity (default 1024), overflow flag, flush channel
       - `NewNotificationQueue(capacity int) *NotificationQueue` constructor
       - `Push(notification DirNotification)` -- adds to queue; if at capacity, sets overflow=true and collapses
       - `Drain() ([]DirNotification, bool)` -- returns all events and overflow flag, resets queue
       - `Len() int` -- returns current event count
       - `FlushCh() <-chan struct{}` -- returns channel signaled on flush threshold (100 events)
       - Thread-safe with sync.Mutex

    5. Extend `pkg/metadata/lock/store.go`:
       - Add delegation fields to `PersistedLock`: `DelegationID string`, `DelegType int`, `IsDirectory bool`, `DelegBreaking bool`, `DelegRecalled bool`, `DelegRevoked bool`, `DelegNotificationMask uint32`
       - Update `ToPersistedLock` to serialize delegation fields from UnifiedLock.Delegation
       - Update `FromPersistedLock` to deserialize delegation fields back into UnifiedLock.Delegation

    6. Create test files:
       - `pkg/metadata/lock/delegation_test.go`: Test Delegation creation, Clone, DelegationConflictsWithLease (all 6 combinations), IsDelegation on UnifiedLock
       - `pkg/metadata/lock/notification_queue_test.go`: Test Push/Drain, overflow collapse, Len, FlushCh signaling, thread safety

    IMPORTANT: Do NOT touch adapter code (internal/adapter/nfs/ or internal/adapter/smb/). Only modify pkg/metadata/lock/ files. Adapters will be updated in Plan 02.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs-cross && go test ./pkg/metadata/lock/... -run "TestDelegation|TestNotification" -v -count=1</automated>
  </verify>
  <done>
    - Delegation struct exists with all fields, no NFS-specific types
    - UnifiedLock has *Delegation field and IsDelegation() method
    - BreakCallbacks has OnDelegationRecall method
    - NotificationQueue handles bounded capacity with overflow collapse
    - PersistedLock round-trips delegation data
    - All new tests pass
    - go build ./pkg/metadata/lock/... passes
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add unified CheckAndBreakCaching methods and extend OpLockBreakScanner for delegations</name>
  <files>
    pkg/metadata/lock/manager.go,
    pkg/metadata/lock/oplock_break.go,
    pkg/metadata/lock/directory.go,
    pkg/metadata/lock/leases.go,
    pkg/metadata/lock/cross_protocol.go
  </files>
  <behavior>
    - LockManager interface has CheckAndBreakCachingForWrite/Read/Delete methods
    - CheckAndBreakCachingForWrite breaks all leases AND all delegations (both read and write)
    - CheckAndBreakCachingForRead breaks write leases AND write delegations (read deleg + read lease coexist)
    - CheckAndBreakCachingForDelete breaks all leases AND all delegations
    - Old CheckAndBreakOpLocksFor* methods become thin wrappers calling the new Caching methods
    - LockManager has GrantDelegation, RevokeDelegation, ReturnDelegation, GetDelegation methods
    - OpLockBreakScanner scans for expired delegation recalls (Breaking=true, BreakStarted + timeout exceeded) and force-revokes them
    - LockManager has WaitForBreakCompletion(ctx, handleKey) for blocking until breaks resolve
    - OnDirChange in directory.go also breaks directory delegations (not just directory leases)
    - Unified anti-storm: recentlyBrokenCache handles both leases and delegations, preventing re-grant for 30s
    - RequestLease in leases.go checks delegation coexistence before granting
  </behavior>
  <action>
    1. Extend `pkg/metadata/lock/manager.go`:
       - Add to LockManager interface: `GrantDelegation(handleKey string, delegation *Delegation) error`, `RevokeDelegation(handleKey string, delegationID string) error`, `ReturnDelegation(handleKey string, delegationID string) error`, `GetDelegation(handleKey string, delegationID string) *Delegation`, `ListDelegations(handleKey string) []*Delegation`
       - Add to LockManager interface: `CheckAndBreakCachingForWrite(handleKey string, excludeOwner *LockOwner) error`, `CheckAndBreakCachingForRead(handleKey string, excludeOwner *LockOwner) error`, `CheckAndBreakCachingForDelete(handleKey string, excludeOwner *LockOwner) error`
       - Add to LockManager interface: `WaitForBreakCompletion(ctx context.Context, handleKey string) error`
       - Implement `GrantDelegation` on Manager: creates UnifiedLock with Delegation field set, adds to unifiedLocks map. Returns error if conflicting lease exists (use DelegationConflictsWithLease). Check recentlyBrokenCache anti-storm.
       - Implement `RevokeDelegation`: marks delegation as Revoked, removes from map, signals break wait channel
       - Implement `ReturnDelegation`: removes delegation lock from map (idempotent -- return nil if not found), signals break wait channel
       - Implement `CheckAndBreakCachingFor*` methods per research Pattern 3: collect both lease and delegation locks to break under lock, release lock, dispatch breaks in parallel. For Write: break all leases to None + all delegations. For Read: break write leases to Read + write delegations. For Delete: break all leases to None + all delegations.
       - Make old `CheckAndBreakOpLocksFor*` call into `CheckAndBreakCachingFor*` (backward compat)
       - Implement `WaitForBreakCompletion`: use per-handleKey channel (map[string]chan struct{}), block on channel or ctx.Done()
       - Add `breakWaitChans map[string]chan struct{}` to Manager struct
       - Add `dispatchDelegationRecall(handleKey string, lock *UnifiedLock)` private method that calls all registered BreakCallbacks.OnDelegationRecall in goroutines

    2. Extend `pkg/metadata/lock/oplock_break.go`:
       - Extend `scanAndBreak()` method in OpLockBreakScanner to also scan for delegations with `Breaking=true` and `BreakStarted + delegationRecallTimeout < now`. Force-revoke expired delegation recalls by calling `RevokeDelegation`.
       - Add `DelegationRecallTimeout time.Duration` field to Manager (default 90s, configurable)
       - The scan interval stays at 1 second (existing `OpLockBreakScanInterval`)

    3. Extend `pkg/metadata/lock/directory.go`:
       - Extend `OnDirChange` to also break directory delegations (not just directory leases). When a directory change occurs, iterate unifiedLocks for parentHandle, find locks with `Delegation != nil && Delegation.IsDirectory`, mark them as Breaking, dispatch OnDelegationRecall via BreakCallbacks.
       - Extend `recentlyBrokenCache` to also track delegation recalls (unified anti-storm per CONTEXT.md decision)

    4. Extend `pkg/metadata/lock/leases.go`:
       - In `requestLeaseImpl`, before granting a lease, check if conflicting delegations exist on the same handleKey using DelegationConflictsWithLease. If conflict, return error or initiate break.

    5. Extend `pkg/metadata/lock/cross_protocol.go`:
       - Add `DelegationConflictReason` and `FormatDelegationConflict` for logging cross-protocol delegation conflicts at INFO level
       - Add helpers for Lease V2 translation (ParentLeaseKey, epoch) if needed for cross-protocol view

    CRITICAL: Release LockManager mutex BEFORE dispatching break callbacks (follow existing pattern in breakOpLocks to avoid deadlock per research Pitfall 2).

    IMPORTANT: Update `RemoveFileUnifiedLocks` to also clean up delegation entries (research Pitfall 5).
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs-cross && go test ./pkg/metadata/lock/... -v -count=1 -timeout 120s</automated>
  </verify>
  <done>
    - LockManager interface has all new delegation and caching methods
    - CheckAndBreakCachingFor* breaks both leases and delegations in parallel
    - Old CheckAndBreakOpLocksFor* still work (backward compat)
    - OpLockBreakScanner handles delegation recall timeouts
    - WaitForBreakCompletion blocks until breaks resolve or ctx cancelled
    - OnDirChange breaks both directory leases and directory delegations
    - Unified anti-storm cache prevents rapid re-grants cross-protocol
    - RequestLease checks delegation coexistence
    - All existing lock package tests still pass
    - All new tests pass
    - go build ./pkg/metadata/lock/... passes
  </done>
</task>

</tasks>

<verification>
- `go build ./pkg/metadata/lock/...` compiles cleanly
- `go test ./pkg/metadata/lock/... -v -count=1` all tests pass
- `go vet ./pkg/metadata/lock/...` no issues
- Delegation struct has zero NFS-specific types (no Stateid4, no *time.Timer)
- BreakCallbacks interface has 4 methods (OnOpLockBreak, OnByteRangeRevoke, OnAccessConflict, OnDelegationRecall)
- LockManager interface has CheckAndBreakCachingFor* methods
- NotificationQueue handles overflow correctly (collapse to single event)
</verification>

<success_criteria>
- Protocol-neutral Delegation struct exists in pkg/metadata/lock/delegation.go
- UnifiedLock extended with *Delegation field
- BreakCallbacks extended with OnDelegationRecall
- Unified CheckAndBreakCaching methods replace oplock-only break methods
- OpLockBreakScanner handles delegation timeouts
- Bounded notification queue with overflow collapse
- WaitForBreakCompletion channel-based waiter
- All tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/39-cross-protocol-integration-and-documentation/39-01-SUMMARY.md`
</output>
