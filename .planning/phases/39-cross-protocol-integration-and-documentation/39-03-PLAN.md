---
phase: 39-cross-protocol-integration-and-documentation
plan: 03
type: execute
wave: 3
depends_on: ["39-02"]
files_modified:
  - docs/SMB.md
  - docs/SECURITY.md
  - docs/CONFIGURATION.md
  - docs/TROUBLESHOOTING.md
  - README.md
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "docs/SMB.md covers all v3.8 features: dialect negotiation, encryption, signing, leases V2, directory leasing, durable handles, Kerberos, cross-protocol coordination"
    - "docs/SECURITY.md has SMB3 security section covering AES encryption, AES-CMAC/GMAC signing, SPNEGO/Kerberos"
    - "docs/CONFIGURATION.md has SMB3 adapter configuration options"
    - "docs/TROUBLESHOOTING.md has cross-protocol troubleshooting section"
    - "README.md mentions SMB3 protocol support with link to docs/SMB.md"
    - "Cross-protocol behavior matrix table exists in docs/SMB.md"
  artifacts:
    - path: "docs/SMB.md"
      provides: "Comprehensive SMB3 protocol documentation"
      min_lines: 1200
    - path: "docs/SECURITY.md"
      provides: "SMB3 security model documentation"
      contains: "AES-128-GCM"
    - path: "docs/CONFIGURATION.md"
      provides: "SMB3 adapter configuration section"
      contains: "smb3"
    - path: "docs/TROUBLESHOOTING.md"
      provides: "Cross-protocol troubleshooting section"
      contains: "cross-protocol"
  key_links:
    - from: "README.md"
      to: "docs/SMB.md"
      via: "Markdown link to SMB documentation"
      pattern: "docs/SMB\\.md"
    - from: "docs/SMB.md"
      to: "docs/SECURITY.md"
      via: "Cross-reference to security documentation"
      pattern: "SECURITY\\.md"
---

<objective>
Expand docs/SMB.md to cover all v3.8 SMB3 features (dialect negotiation, encryption, signing, leases V2, directory leasing, durable handles, Kerberos, cross-protocol coordination), update docs/SECURITY.md with SMB3 security model, update docs/CONFIGURATION.md with SMB3 adapter options, add cross-protocol troubleshooting to docs/TROUBLESHOOTING.md, and add SMB3 mention to README.md.

Purpose: Comprehensive documentation enables users to configure and troubleshoot SMB3, and maintainers to understand wire format details and cross-protocol behavior.

Output: Updated documentation files covering all v3.8 features.
</objective>

<execution_context>
@/Users/marmos91/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marmos91/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-cross-protocol-integration-and-documentation/39-CONTEXT.md
@.planning/phases/39-cross-protocol-integration-and-documentation/39-RESEARCH.md
@.planning/phases/39-cross-protocol-integration-and-documentation/39-02-SUMMARY.md

@docs/SMB.md
@docs/SECURITY.md
@docs/CONFIGURATION.md
@docs/TROUBLESHOOTING.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand docs/SMB.md with comprehensive SMB3 documentation</name>
  <files>docs/SMB.md</files>
  <action>
    Expand the existing docs/SMB.md (currently 741 lines, covers SMB2 only) to include all v3.8 SMB3 features. Follow the existing documentation style (operational + wire format details for maintainers).

    Add the following NEW sections (keeping existing content and updating the overview/introduction to reflect SMB3 support):

    1. **Update Protocol Overview**: Change "SMB2 dialect 0x0202" to reflect multi-dialect support (2.0.2, 3.0, 3.0.2, 3.1.1). Update the SMB vs NFS comparison table to include SMB3 columns.

    2. **SMB3 Dialect Negotiation** section:
       - Dialect selection process (highest mutually supported)
       - Negotiate contexts: PREAUTH_INTEGRITY_CAPABILITIES (SHA-512), ENCRYPTION_CAPABILITIES (AES-128-GCM, AES-128-CCM, AES-256-GCM, AES-256-CCM), SIGNING_CAPABILITIES (AES-128-CMAC, AES-128-GMAC)
       - Preauth integrity hash chain (SHA-512 over raw wire bytes)
       - Server cipher/signing preference order
       - FSCTL_VALIDATE_NEGOTIATE_INFO for downgrade protection (3.0/3.0.2 only; 3.1.1 drops TCP)

    3. **Encryption** section:
       - AES-128-GCM (default for 3.1.1), AES-128-CCM (3.0/3.0.2 default)
       - AES-256-GCM, AES-256-CCM variants
       - Transform header (0xFD) framing
       - Per-session encryption (Session.EncryptData)
       - Per-share encryption (Share.EncryptData)
       - Key derivation for encryption keys

    4. **Signing** section:
       - HMAC-SHA256 (SMB 2.x), AES-128-CMAC (SMB 3.0+), AES-128-GMAC (SMB 3.1.1)
       - Signing algorithm selection by dialect
       - SP800-108 Counter Mode KDF for signing keys
       - When signing is required vs optional

    5. **Leases V2 and Directory Leasing** section:
       - Lease V2 vs V1: ParentLeaseKey, epoch tracking
       - Lease state machine (None, Read, Read-Handle, Read-Write, Read-Write-Handle)
       - Directory leases (Read-caching for directory listings)
       - Directory lease breaks on content changes (create/delete/rename)
       - Epoch-based stale break prevention

    6. **Durable Handles** section:
       - V1 (DHnQ/DHnC) with batch oplock requirement
       - V2 (DH2Q/DH2C) with CreateGuid for idempotent reconnection
       - Reconnect validation (14+ checks per MS-SMB2 spec)
       - Handle timeout and scavenger
       - App Instance ID for cluster failover scenarios

    7. **Kerberos/SPNEGO Authentication** section:
       - SPNEGO negotiation flow
       - Kerberos session setup with AP-REQ/AP-REP
       - Session key extraction for KDF
       - NTLM fallback
       - Guest sessions (no encryption/signing)
       - Keytab management and hot-reload

    8. **Cross-Protocol Behavior** section:
       - Cross-protocol behavior matrix table (CRITICAL per CONTEXT.md):
         ```
         | NFS Operation | SMB Lease Effect | SMB Dir Lease Effect |
         | SMB Operation | NFS Delegation Effect | NFS Dir Delegation Effect |
         ```
       - File caching coexistence rules: NFS read delegation + SMB Read lease coexist; write exclusive
       - Break flow: SMB write -> LockManager -> OnDelegationRecall -> CB_RECALL to NFS client
       - Break flow: NFS open -> LockManager -> OnOpLockBreak -> SMB lease break notification
       - Directory changes: create/delete/rename -> MetadataService -> LockManager -> breaks both dir leases and dir delegations
       - Anti-storm mechanism: unified 30s RecentlyRecalledTTL prevents rapid re-grants
       - Notification queue: batched directory change events consumed by CB_NOTIFY and CHANGE_NOTIFY

    9. **Key Derivation** section:
       - SP800-108 Counter Mode KDF algorithm
       - SMB 3.0/3.0.2: constant label/context strings
       - SMB 3.1.1: preauth integrity hash as KDF context
       - Key purposes: SigningKey, EncryptionKey, DecryptionKey, ApplicationKey

    10. Update **Table of Contents** to include all new sections.

    11. Update **Known Limitations** to mention what SMB3 features are not yet supported (compression, multichannel, persistent handles, RDMA, QUIC).

    Style guidelines:
    - Both operational (how to use/configure) AND wire format details (for maintainers)
    - Use tables for comparison/matrix data
    - Include configuration examples using YAML snippets
    - Cross-reference docs/SECURITY.md for detailed security model
    - Cross-reference docs/CONFIGURATION.md for full config options
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs-cross && wc -l docs/SMB.md | awk '{if ($1 >= 1200) print "PASS: "$1" lines"; else print "FAIL: only "$1" lines"}'</automated>
  </verify>
  <done>
    - docs/SMB.md covers all v3.8 features (dialect negotiation, encryption, signing, leases V2, directory leasing, durable handles, Kerberos, cross-protocol)
    - Cross-protocol behavior matrix table included
    - Both operational and wire format details present
    - At least 1200 lines (significant expansion from 741)
    - All new sections have Table of Contents entries
    - Known Limitations updated with SMB3 scope
  </done>
</task>

<task type="auto">
  <name>Task 2: Update SECURITY.md, CONFIGURATION.md, TROUBLESHOOTING.md, and README.md</name>
  <files>docs/SECURITY.md, docs/CONFIGURATION.md, docs/TROUBLESHOOTING.md, README.md</files>
  <action>
    1. **docs/SECURITY.md** -- Add "SMB3 Security Model" section:
       - AES encryption: cipher suites (AES-128-GCM, AES-128-CCM, AES-256-GCM, AES-256-CCM), when each is used
       - AES signing: CMAC (3.0+) and GMAC (3.1.1), algorithm negotiation
       - SPNEGO/Kerberos: session setup flow, mutual authentication (AP-REP), session key derivation
       - Preauth integrity: SHA-512 hash chain preventing downgrade attacks
       - FSCTL_VALIDATE_NEGOTIATE_INFO: dialect validation for 3.0/3.0.2
       - Guest sessions: no encryption, no signing, security implications
       - NTLM fallback: when it occurs, security tradeoffs
       - Transport security comparison: SMB3 encryption vs NFS Kerberos krb5p
       - Key derivation: SP800-108 KDF, how keys are derived from session key
       - Per-session vs per-share encryption enforcement

    2. **docs/CONFIGURATION.md** -- Add "SMB3 Adapter Configuration" section:
       - Dialect selection: min/max dialect settings
       - Encryption configuration: enforcement modes (required, optional, disabled), cipher preference
       - Signing configuration: requirement level, algorithm preference (GMAC > CMAC > HMAC-SHA256)
       - Lease configuration: timeouts, directory lease settings
       - Durable handle configuration: timeout, scavenger interval
       - Kerberos configuration: keytab path, service principal, hot-reload
       - Cross-protocol settings: delegation recall timeout (90s default), anti-storm TTL (30s)
       - Complete YAML example for SMB3 adapter section
       - Environment variable overrides for all SMB3 settings

    3. **docs/TROUBLESHOOTING.md** -- Add "Cross-Protocol Issues" section:
       - "File locked by another protocol": diagnosis and resolution
       - "Delegation recall timeouts": when NFS client doesn't respond to CB_RECALL
       - "Lease break storms": rapid grant-break-grant cycles, anti-storm cache
       - "SMB client can't write to NFS-delegated file": expected behavior, wait for delegation return
       - "NFS client sees stale data after SMB write": delegation recall + cache invalidation flow
       - "Directory listing inconsistency across protocols": directory lease/delegation break coordination
       - Diagnostic commands: checking active locks, delegations, leases via dfsctl
       - Log messages to look for: cross_protocol_break, delegation_recall, lease_break at INFO level

    4. **README.md** -- Add brief SMB3 mention:
       - In the protocol overview section, update to mention SMB3 support (dialects 3.0, 3.0.2, 3.1.1)
       - Add link to docs/SMB.md for full SMB3 documentation
       - Mention key SMB3 features: encryption, signing, leases, durable handles, Kerberos
       - Keep it brief (3-5 lines) -- README should point to detailed docs

    Style: Follow existing documentation style in each file. Use consistent heading levels, code block formatting, and cross-references.
  </action>
  <verify>
    <automated>cd /Users/marmos91/Projects/dittofs-cross && grep -c "SMB3\|smb3\|SMB 3" docs/SECURITY.md docs/CONFIGURATION.md docs/TROUBLESHOOTING.md README.md | awk -F: '{sum+=$2} END {if (sum >= 20) print "PASS: "sum" SMB3 references"; else print "FAIL: only "sum" references"}'</automated>
  </verify>
  <done>
    - docs/SECURITY.md has SMB3 security model section (AES encryption, CMAC/GMAC signing, SPNEGO/Kerberos)
    - docs/CONFIGURATION.md has SMB3 adapter configuration section with complete YAML example
    - docs/TROUBLESHOOTING.md has cross-protocol troubleshooting section with diagnostic guidance
    - README.md mentions SMB3 with link to docs/SMB.md
    - All documentation cross-references consistent
    - No broken internal links
  </done>
</task>

</tasks>

<verification>
- docs/SMB.md expanded to 1200+ lines covering all v3.8 features
- docs/SECURITY.md has SMB3 security section
- docs/CONFIGURATION.md has SMB3 configuration section
- docs/TROUBLESHOOTING.md has cross-protocol troubleshooting
- README.md mentions SMB3 with link
- Cross-protocol behavior matrix table present in SMB.md
- All files maintain consistent documentation style
</verification>

<success_criteria>
- All v3.8 SMB3 features documented (dialect negotiation, encryption, signing, leases V2, directory leasing, durable handles, Kerberos, cross-protocol)
- Cross-protocol behavior matrix table in docs/SMB.md
- Security, configuration, and troubleshooting docs updated
- README.md has brief SMB3 mention with link
- Documentation follows existing style conventions
</success_criteria>

<output>
After completion, create `.planning/phases/39-cross-protocol-integration-and-documentation/39-03-SUMMARY.md`
</output>
