---
phase: 40-smb3-conformance-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/smb-conformance/smbtorture/KNOWN_FAILURES.md
  - test/smb-conformance/smbtorture/run.sh
  - test/smb-conformance/smbtorture/baseline-results.md
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "smbtorture runs all SMB2 and SMB3 sub-suites and produces a full result set"
    - "Every wildcard entry in smbtorture KNOWN_FAILURES.md is replaced with individual test names"
    - "A baseline document captures pass/fail counts per sub-suite after phases 33-39"
  artifacts:
    - path: "test/smb-conformance/smbtorture/KNOWN_FAILURES.md"
      provides: "Individual test name enumeration (no wildcards)"
      contains: "no .* wildcard patterns"
    - path: "test/smb-conformance/smbtorture/baseline-results.md"
      provides: "Baseline pass/fail counts per smbtorture sub-suite"
  key_links:
    - from: "test/smb-conformance/smbtorture/run.sh"
      to: "test/smb-conformance/smbtorture/KNOWN_FAILURES.md"
      via: "parse-results.sh reads known failures to determine pass/fail"
      pattern: "KNOWN_FAILURES"
---

<objective>
Re-measure the smbtorture baseline after phases 33-39 (SMB3 implementation) and replace all wildcard entries in KNOWN_FAILURES.md with individually enumerated test names.

Purpose: Phases 33-39 implemented SMB3 dialect negotiation, key derivation, signing, encryption, Kerberos, leases, durable handles, and cross-protocol coordination. The smbtorture known failures list uses wildcard patterns (e.g., `smb2.session.*`) that mask improvements. This plan establishes the true baseline and ensures every known failure is individually verified.

Output: Updated KNOWN_FAILURES.md with individual test names, baseline-results.md documenting pass/fail counts per sub-suite.
</objective>

<execution_context>
@/Users/marmos91/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marmos91/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-smb3-conformance-testing/40-CONTEXT.md
@.planning/phases/40-smb3-conformance-testing/40-RESEARCH.md

@test/smb-conformance/smbtorture/run.sh
@test/smb-conformance/smbtorture/KNOWN_FAILURES.md
@test/smb-conformance/smbtorture/parse-results.sh

<interfaces>
<!-- Existing smbtorture infrastructure. Executor should understand these patterns. -->

From test/smb-conformance/smbtorture/run.sh:
- run.sh accepts --profile (memory|memory-fs|badger-fs), --filter (sub-suite), --timeout, --keep, --verbose, --dry-run
- Full suite mode runs STANDALONE_TESTS and SUITES arrays with prefix normalization
- Output goes to results/ directory as smbtorture-output.txt
- parse-results.sh compares output against KNOWN_FAILURES.md

From test/smb-conformance/smbtorture/KNOWN_FAILURES.md:
- Wildcard patterns (e.g., `smb2.session.*`) match any test with that prefix
- parse-results.sh reads test names from first column of markdown tables
- Currently ALL sub-suites use wildcards -- every entry needs individual enumeration

From test/smb-conformance/docker-compose.yml:
- smbtorture service uses quay.io/samba.org/samba-toolbox:v0.8
- DittoFS built from Dockerfile.dittofs, bootstrapped via bootstrap.sh
- Runs on host network mode for localhost connectivity
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full smbtorture baseline and capture individual test results</name>
  <files>test/smb-conformance/smbtorture/baseline-results.md</files>
  <action>
Run the full smbtorture suite with memory profile against the current DittoFS codebase (which now includes phases 33-39 SMB3 features). Capture EVERY individual test result (pass/fail/skip/error) from the output.

Steps:
1. Run `cd test/smb-conformance && ./smbtorture/run.sh --profile memory --verbose` and capture all output
2. From the smbtorture-output.txt in the results directory, extract every individual test result line (format: `success: testname` or `failure: testname` or `error: testname` or `skip: testname`)
3. Group results by sub-suite (smb2.connect, smb2.session, smb2.lease, smb2.durable-v2-open, etc.)
4. Create `test/smb-conformance/smbtorture/baseline-results.md` documenting:
   - Date and DittoFS commit hash
   - Total tests: pass/fail/skip/error counts
   - Per-sub-suite breakdown table with pass/fail counts
   - List of NEWLY PASSING tests (tests that were previously covered by wildcards but now pass due to phases 33-39)
   - List of all individual failing test names (these become candidates for KNOWN_FAILURES enumeration)
5. The baseline-results.md should clearly identify which wildcard categories now have passing tests (e.g., "smb2.session.*: 3/15 now passing, 12 still failing")

IMPORTANT: Run this on an x86_64 Linux environment (CI or similar). If running locally on ARM64, expect slower execution and potential emulation artifacts. Use `--timeout 1200` to allow sufficient time.

If the smbtorture container is not available locally, build it first: `cd test/smb-conformance && docker compose build`
  </action>
  <verify>
    <automated>ls test/smb-conformance/smbtorture/baseline-results.md && grep -c "| " test/smb-conformance/smbtorture/baseline-results.md</automated>
  </verify>
  <done>baseline-results.md exists with per-sub-suite pass/fail counts and individual test names for all failures</done>
</task>

<task type="auto">
  <name>Task 2: Replace all wildcard entries with individual test names in KNOWN_FAILURES.md</name>
  <files>test/smb-conformance/smbtorture/KNOWN_FAILURES.md</files>
  <action>
Using the baseline results from Task 1, replace every wildcard pattern in KNOWN_FAILURES.md with individually enumerated test names.

Steps:
1. Read the current KNOWN_FAILURES.md which uses patterns like `smb2.session.*`, `smb2.lease.*`, `smb2.durable-v2-open.*`, etc.
2. For each wildcard entry, look up the individual test results from the baseline run:
   - If ALL tests in the suite FAIL: list each individual test name as a known failure with specific reason
   - If SOME tests PASS: list ONLY the failing tests as known failures. The passing tests are NO LONGER known failures.
   - If ALL tests PASS: REMOVE the entry entirely -- this suite is now conformant
3. For each individual known failure entry, verify the reason is genuinely "unimplemented feature" (not a fixable bug):
   - smb2.multichannel.* -- Multi-channel: genuinely unimplemented, keep all individual tests
   - smb2.streams.* -- ADS: genuinely unimplemented, keep all individual tests
   - smb2.twrp.* -- Previous versions: genuinely unimplemented, keep all individual tests
   - smb2.session.* -- Many session tests should NOW PASS with SMB3 sessions (phase 33-39). Only keep tests that fail due to genuinely missing features (e.g., session binding = multichannel)
   - smb2.lease.* -- Leases are now implemented (phase 37). Most should pass. Only keep tests that fail due to lease features not implemented (e.g., V1-only edge cases)
   - smb2.durable-v2-open.* -- Durable handles V2 now implemented (phase 38). Most should pass. Keep only tests requiring missing features
   - smb2.replay.* -- Replay detection: evaluate whether phases 33-39 implemented enough for some tests to pass
   - smb2.credits.* -- Evaluate whether credit management is now sufficient
4. Update the table format to include specific test names (no `.*` suffix):
   ```
   | smb2.session.bind1 | Session binding | Multi-channel not implemented | - |
   | smb2.session.bind2 | Session binding | Multi-channel not implemented | - |
   ```
5. Preserve the section structure (categories) but update category names if warranted
6. Update the "Last updated" line to reflect the current date and phase
7. Remove the "How to Add New Entries" section instruction about using `.*` suffix -- instead instruct to always use exact test names

CRITICAL: Do NOT blindly add all failing tests. Each failure must have a genuine reason (unimplemented feature). If a test fails but the feature IS implemented (e.g., lease tests after phase 37), that test should NOT be in KNOWN_FAILURES -- it should be tracked as a bug to fix in subsequent plans.

Keep a separate list of "failures that need fixing" (tests for implemented features that still fail) -- document these in baseline-results.md under a "Fix Candidates" section.
  </action>
  <verify>
    <automated>! grep -E '\.\*' test/smb-conformance/smbtorture/KNOWN_FAILURES.md | grep -v '^#' | grep -v 'How to' | grep -v '```' && echo "No wildcards found - PASS"</automated>
  </verify>
  <done>KNOWN_FAILURES.md contains only individual test names with verified reasons. No wildcard patterns remain. Tests for implemented features (sessions, leases, durable handles) are NOT in KNOWN_FAILURES unless the specific sub-feature is genuinely unimplemented.</done>
</task>

</tasks>

<verification>
1. `test/smb-conformance/smbtorture/baseline-results.md` exists with per-sub-suite pass/fail counts
2. `test/smb-conformance/smbtorture/KNOWN_FAILURES.md` has zero wildcard patterns (`.*` suffix)
3. Every entry in KNOWN_FAILURES.md has a verified reason (unimplemented feature, not fixable bug)
4. Newly passing tests (from phases 33-39 features) are NOT in KNOWN_FAILURES
5. Run `./smbtorture/run.sh --profile memory` passes (all failures are in the updated KNOWN_FAILURES)
</verification>

<success_criteria>
- smbtorture baseline captured with individual test-level granularity
- All wildcard entries replaced with specific test names
- Tests for implemented features (sessions, leases, durable handles, encryption) removed from KNOWN_FAILURES
- Fix candidates documented for subsequent plans
</success_criteria>

<output>
After completion, create `.planning/phases/40-smb3-conformance-testing/40-01-SUMMARY.md`
</output>
